# 第一章第十节：推广到无限维

> 对应 Shankar Chapter 1, Section 1.10
> 前置知识：[05_第一章_本征值问题.md](05_第一章_本征值问题.md)

---

## 从有限维到连续

有限维离散基 → 连续基（位置 $x\in\mathbb{R}$）：

| 有限维（离散） | 无穷维（连续） |
|---|---|
| 展开 $\vert\psi\rangle=\sum_i c_i\vert e_i\rangle$ | $\vert\psi\rangle=\int\psi(x)\vert x\rangle\,dx$ |
| 系数 $c_i=\langle e_i\vert\psi\rangle$ | **波函数** $\psi(x)=\langle x\vert\psi\rangle$ |
| 完备性 $\sum_i\vert e_i\rangle\langle e_i\vert=\hat{I}$ | $\int\vert x\rangle\langle x\vert\,dx=\hat{I}$ |
| 正交性 $\langle e_i\vert e_j\rangle=\delta_{ij}$ | $\langle x\vert x'\rangle=\delta(x-x')$ |
| 内积 $\langle\phi\vert\psi\rangle=\sum_i c_i^*d_i$ | $\langle\phi\vert\psi\rangle=\int\phi^*(x)\psi(x)\,dx$ |

**规律**：把 $\sum_i$ 换成 $\int dx$，把 $\delta_{ij}$ 换成 $\delta(x-x')$。

---

## 核心认识：波函数是展开系数

$$\boxed{\psi(x) = \langle x\vert\psi\rangle}$$

波函数 $\psi(x)$ 就是量子态 $\vert\psi\rangle$ 在位置基下的展开系数。$\vert\psi\rangle$ 和 $\psi(x)$ 是同一件事的两种写法。

---

## Dirac delta 函数

$\delta(x-x')$：在 $x=x'$ 处无穷高无穷窄的尖峰，面积为 1。

**筛选性质**（最重要）：

$$\int_{-\infty}^{+\infty}\delta(x-x')f(x')\,dx' = f(x)$$

delta 函数从所有 $x'$ 中"筛出" $x'=x$ 处的值。

这是连续版的 $\delta_{ij}$：$\langle x\vert x'\rangle=\delta(x-x')$。

验证完备性：$\int\vert x\rangle\langle x\vert dx\cdot\vert\psi\rangle = \int\psi(x)\vert x\rangle\,dx = \vert\psi\rangle$ ✓

---

## 位置算符和动量算符

**位置算符** $\hat{X}$：

$$\hat{X}\psi(x) = x\cdot\psi(x)$$

$\vert x\rangle$ 是本征向量，本征值 $x$（测量结果是位置 $x$）。

**动量算符** $\hat{P}$：

$$\hat{P}\psi(x) = -i\hbar\frac{d\psi}{dx}$$

$\hbar\approx1.055\times10^{-34}$ J·s，约化普朗克常数。

---

## 对易关系：$[\hat{X},\hat{P}]=i\hbar$

作用在任意 $\psi(x)$ 上推导：

$$[\hat{X},\hat{P}]\psi = \hat{X}(\hat{P}\psi)-\hat{P}(\hat{X}\psi)$$

$$= x\!\left(-i\hbar\frac{d\psi}{dx}\right) - \left(-i\hbar\frac{d(x\psi)}{dx}\right)$$

$$= -i\hbar x\psi' + i\hbar(\psi + x\psi') = i\hbar\psi$$

因此：$\boxed{[\hat{X},\hat{P}]=i\hbar}$

---

## 海森堡不确定性原理

$[\hat{X},\hat{P}]\neq 0$ 直接导致：

$$\Delta x\cdot\Delta p \geq \frac{\hbar}{2}$$

这是大自然的基本规律，不是测量精度的问题。

---

## 第一章总结

```
第一章数学框架
├── 向量空间：函数可以是向量
├── 内积：⟨ψ|φ⟩ 测量两态的"重叠"
├── Dirac 符号：|ψ⟩（ket）和 ⟨ψ|（bra）
├── 算符 → 物理量
│     ├── 厄米 → 本征值实数 → 测量结果
│     └── 幺正 → 保范数 → 时间演化
├── 本征值：Â|a⟩=a|a⟩
│     ├── 本征值 a = 可能的测量结果
│     └── 概率 = |⟨a|ψ⟩|²
└── 无穷维
      ├── 波函数 ψ(x)=⟨x|ψ⟩
      ├── X̂：乘以 x
      ├── P̂：-iℏ d/dx
      └── [X̂,P̂]=iℏ → 不确定性原理
```

---

## 练习

1. 用筛选性质算 $\int_{-\infty}^{+\infty}\delta(x-3)\cdot x^2\,dx$

2. 验证 $\psi(x)=e^{ikx}$ 是 $\hat{P}=-i\hbar\frac{d}{dx}$ 的本征函数，求本征值

3. 已知 $[\hat{A},\hat{B}]=i\hbar\hat{I}$，求 $[\hat{B},\hat{A}]$

<details>
<summary>答案</summary>

1. $\delta(x-3)$ 筛出 $x=3$：$x^2\big|_{x=3}=9$

2. $\hat{P}e^{ikx}=-i\hbar(ik)e^{ikx}=\hbar k\cdot e^{ikx}$，本征值 $p=\hbar k$（德布罗意关系）

3. $[\hat{B},\hat{A}]=-[\hat{A},\hat{B}]=-i\hbar\hat{I}$

</details>

---

下一章：[07_第二章_经典力学回顾.md](07_第二章_经典力学回顾.md)

Shankar 第二章回顾经典力学（拉格朗日和哈密顿力学），为进入量子力学做准备。
