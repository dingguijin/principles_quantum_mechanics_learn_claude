# 第一章第一节：线性向量空间（Linear Vector Spaces）

> **对应 Shankar**：Chapter 1, Section 1.1  
> **前置知识**：[00_预备课_复数.md](00_预备课_复数.md)

---

## 从你已经知道的向量说起

高中学过向量。在平面上，一个向量是一个**带方向的箭头**：

```
        ↑ y
        |
        |    /→ v = (3, 2)
        |   /
        |  /
        | /
        |/
--------+----------→ x
```

向量 $\vec{v} = (3, 2)$ 表示：向右走 3，向上走 2。

两个基本操作：

**加法**：
$$\vec{v} + \vec{w} = (3+1,\ 2+(-1)) = (4, 1)$$

**数乘**：
$$2 \cdot \vec{v} = (6, 4)$$

Shankar 要做的事：**把这个直觉彻底抽象化——忘掉"箭头"，只保留"规则"。**

---

## 线性向量空间的定义

一个**线性向量空间**（linear vector space）$V$ 是一个集合，元素叫**向量**，有两种操作（加法、数乘），满足以下 8 条规则：

设 $|a\rangle, |b\rangle, |c\rangle \in V$，$\alpha, \beta \in \mathbb{C}$（复数）：

| 编号 | 规则 | 符号 |
|------|------|------|
| 1 | 加法交换律 | $\|a\rangle + \|b\rangle = \|b\rangle + \|a\rangle$ |
| 2 | 加法结合律 | $(\|a\rangle + \|b\rangle) + \|c\rangle = \|a\rangle + (\|b\rangle + \|c\rangle)$ |
| 3 | 存在零向量 | 存在 $\|0\rangle$ 使得 $\|a\rangle + \|0\rangle = \|a\rangle$ |
| 4 | 存在负向量 | 对每个 $\|a\rangle$，存在 $\|-a\rangle$ 使得 $\|a\rangle + \|-a\rangle = \|0\rangle$ |
| 5 | 数乘对向量加法分配 | $\alpha(\|a\rangle + \|b\rangle) = \alpha\|a\rangle + \alpha\|b\rangle$ |
| 6 | 数乘对标量加法分配 | $(\alpha+\beta)\|a\rangle = \alpha\|a\rangle + \beta\|a\rangle$ |
| 7 | 数乘结合律 | $\alpha(\beta\|a\rangle) = (\alpha\beta)\|a\rangle$ |
| 8 | 单位元 | $1 \cdot \|a\rangle = \|a\rangle$ |

### 狄拉克符号（Dirac / bra-ket notation）

$|a\rangle$ 读作 "ket a"（右矢），是量子力学专用记法，暂时理解为"名字叫 $a$ 的向量"。

---

## 三个向量空间的例子

### 例子 1：平面向量 $\mathbb{R}^2$（你熟悉的）

所有 $(x, y)$，$x, y \in \mathbb{R}$，按分量加法和数乘。✅

### 例子 2：多项式

所有 $f(x) = a + bx + cx^2$，$a,b,c \in \mathbb{R}$，按普通加法和数乘。✅  
多项式是向量！

### 例子 3：复值函数（量子力学的核心！）

所有函数 $\psi(x): \mathbb{R} \to \mathbb{C}$，按逐点加法和数乘。✅  
**函数是向量！** 量子态（波函数）就住在这里。

---

## 线性组合（Linear Combination）

$$\alpha_1 |v_1\rangle + \alpha_2 |v_2\rangle + \cdots + \alpha_n |v_n\rangle$$

叫做 $|v_1\rangle, \ldots, |v_n\rangle$ 的一个**线性组合**，$\alpha_i$ 为复数系数。

---

## 线性无关（Linear Independence）

一组向量 $|v_1\rangle, \ldots, |v_n\rangle$ **线性无关**，当且仅当：

$$\alpha_1|v_1\rangle + \cdots + \alpha_n|v_n\rangle = |0\rangle \implies \alpha_1 = \cdots = \alpha_n = 0$$

**直觉**：线性相关 = 有冗余（某个向量能被其他向量表示）。

**例子**：$(1,0,0)$、$(0,1,0)$、$(1,1,0)$ 线性相关，因为第三个 $= $ 前两个之和。

---

## 基（Basis）与维度（Dimension）

**基**：一组满足以下两条的向量：
1. 线性无关（无冗余）
2. 张成整个空间（任意向量都能表示为线性组合）

**维度**：基里向量的个数。

$\mathbb{R}^2$ 的标准基：$(1,0)$ 和 $(0,1)$，维度 = **2**。  
基不唯一，但维度对同一空间是固定的。

---

## 本节核心

```
向量空间
  ├── 元素叫向量（不一定是箭头——函数也是向量！）
  ├── 两种操作：加法、数乘（满足 8 条规则）
  ├── 线性组合：用标量系数把多个向量加在一起
  ├── 线性无关：没有向量是其他向量的线性组合
  ├── 基：线性无关 + 张成整个空间
  └── 维度：基里向量的个数
```

量子力学的状态空间是**无穷维**函数向量空间，但这些概念全部适用。

---

## 小测试

1. 以下哪个是向量空间？
   - a) 所有正实数（普通加法和数乘）
   - b) 所有 $2 \times 2$ 实数矩阵

2. 在 $\mathbb{R}^3$ 中，$(1,0,0)$、$(0,1,0)$、$(1,1,0)$ 线性无关吗？

3. $\mathbb{R}^3$ 的维度是多少？标准基是什么？

<details>
<summary>点击查看答案</summary>

1. **a) 不是**：$(-1) \times 1 = -1$ 不是正实数，数乘不封闭。**b) 是**：矩阵的加法和数乘满足全部 8 条规则。

2. **线性相关**：$(1,1,0) = (1,0,0) + (0,1,0)$，有冗余。

3. 维度 **3**，标准基：$(1,0,0)$、$(0,1,0)$、$(0,0,1)$。

</details>

---

## 下一节

[02_第一章_内积空间.md](02_第一章_内积空间.md)

引入"长度"和"角度"的概念——**内积空间**，这是量子力学计算概率的数学基础。
