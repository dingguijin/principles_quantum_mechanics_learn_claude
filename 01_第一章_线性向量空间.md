# 第一章第一节：线性向量空间（Linear Vector Spaces）

> 对应 Shankar Chapter 1, Section 1.1
> 前置知识：[00_预备课_复数.md](00_预备课_复数.md)

---

## 从箭头出发

高中学过的向量：平面上带方向的箭头，比如 $\vec{v} = (3,2)$ 表示"向右 3、向上 2"。

这种箭头能做两件事：

**加法**：$(3,2)+(1,3) = (4,5)$，分量分别相加

**数乘**：$2\cdot(3,2) = (6,4)$，每个分量乘以这个数

---

## 关键发现：多项式也满足完全相同的规律

多项式 $f(x) = 1+2x+3x^2$ 和 $g(x) = 2-x+x^2$ 也能相加（系数相加）和数乘（系数乘以标量），并且满足和箭头向量**完全相同的计算规律**（交换律、分配律等）。

函数也一样：$\psi_1(x)+\psi_2(x)$ 每点处相加，$\alpha\psi(x)$ 每点处乘以 $\alpha$，规律相同。

**数学家的做法**：把这些共同规律提炼成一个定义，取名"向量空间"，这样对这个定义证明一次，所有满足它的东西都自动继承。

---

## 向量空间的定义

一个**向量空间** $V$：一个集合（元素叫向量），配备加法和数乘（标量为复数），满足 8 条规律：

设 $\vert a\rangle,\vert b\rangle,\vert c\rangle\in V$，$\alpha,\beta\in\mathbb{C}$：

> 符号说明：$\vert a\rangle$（读"ket a"）是量子力学标准向量写法，暂时理解为"名叫 $a$ 的向量"。

| 规律 | 内容 |
|------|------|
| A1 加法交换律 | $\vert a\rangle+\vert b\rangle = \vert b\rangle+\vert a\rangle$ |
| A2 加法结合律 | $(\vert a\rangle+\vert b\rangle)+\vert c\rangle = \vert a\rangle+(\vert b\rangle+\vert c\rangle)$ |
| A3 零向量存在 | 存在 $\vert 0\rangle$，使 $\vert a\rangle+\vert 0\rangle=\vert a\rangle$ |
| A4 负向量存在 | 对每个 $\vert a\rangle$，存在 $\vert{-a}\rangle$，使 $\vert a\rangle+\vert{-a}\rangle=\vert 0\rangle$ |
| S1 数乘对向量加法分配 | $\alpha(\vert a\rangle+\vert b\rangle)=\alpha\vert a\rangle+\alpha\vert b\rangle$ |
| S2 数乘对标量加法分配 | $(\alpha+\beta)\vert a\rangle=\alpha\vert a\rangle+\beta\vert a\rangle$ |
| S3 数乘结合律 | $\alpha(\beta\vert a\rangle)=(\alpha\beta)\vert a\rangle$ |
| S4 单位元 | $1\cdot\vert a\rangle=\vert a\rangle$ |

**封闭性**（隐含要求）：加法和数乘的结果必须还在 $V$ 里。

---

## 三个例子

**$\mathbb{R}^2$**：所有 $(x,y)$，$x,y\in\mathbb{R}$。加法和数乘按分量计算。8 条全满足。

**次数 $\leq 2$ 的多项式**：$a+bx+cx^2$，系数实数。按系数对应计算。8 条全满足。维度 3。

**复值函数**（量子力学核心！）：所有 $\psi:\mathbb{R}\to\mathbb{C}$。逐点加法和数乘。8 条全满足。**维度无穷大。**

> 量子力学中，粒子状态用波函数 $\psi(x)$ 描述。波函数就住在这个函数向量空间里。

---

## 反例：正实数不是向量空间

正实数的数乘不封闭：$(-1)\times 3 = -3$ 不是正实数。A4 也不满足：$3$ 的负向量 $-3$ 不在集合里。

---

## 线性组合

$$\alpha_1\vert v_1\rangle+\alpha_2\vert v_2\rangle+\cdots+\alpha_n\vert v_n\rangle$$

叫做这些向量的一个**线性组合**，$\alpha_i$ 是复数系数。

例：$(3,5) = 3(1,0)+5(0,1)$

---

## 线性无关：检验有无冗余

**检验方法**：令 $\alpha_1\vert v_1\rangle+\cdots+\alpha_n\vert v_n\rangle = \vert 0\rangle$，看系数是否必须全为零。

- 必须全为零 → **线性无关**（无冗余）
- 存在非零解 → **线性相关**（有冗余）

**例**：$(1,0)$ 和 $(0,1)$：令 $\alpha(1,0)+\beta(0,1)=(0,0)$，得 $\alpha=\beta=0$。线性无关。

**例**：$(1,0),(0,1),(2,3)$：令各系数为 $\alpha,\beta,\gamma$，取 $\gamma=1,\alpha=-2,\beta=-3$ 是非零解。线性相关（$(2,3)$ 冗余）。

---

## 基与维度

**基**：线性无关 + 能表示空间里所有向量的一组向量。

**维度**：基里向量的个数（对同一空间，不管选哪组基，这个数都相同）。

$\mathbb{R}^2$ 的标准基：$(1,0)$ 和 $(0,1)$，维度 **2**。

函数空间：无穷维。

---

## 练习

1. 验证 $(1,0,0),(0,1,0),(0,0,1)$ 在 $\mathbb{R}^3$ 中线性无关。
2. $(1,2)$ 和 $(2,4)$ 线性相关吗？说明理由。
3. 以下哪个是向量空间？
   - a) 所有满足 $x+y=0$ 的点 $(x,y)$
   - b) 所有满足 $x+y=1$ 的点 $(x,y)$

<details>
<summary>答案</summary>

1. $\alpha(1,0,0)+\beta(0,1,0)+\gamma(0,0,1)=(0,0,0)$ 只有零解，线性无关。

2. $(2,4)=2\cdot(1,2)$，即 $2(1,2)+(-1)(2,4)=(0,0)$，非零系数解存在，**线性相关**。

3. a) **是**：零向量满足，加法数乘封闭（$x+y=0$ 且 $x'+y'=0$，则 $(x+x')+(y+y')=0$）。b) **不是**：$(0,0)$ 不满足 $x+y=1$，没有零向量。

</details>

---

下一节：[02_第一章_内积空间.md](02_第一章_内积空间.md)
