# 第一章第一节：线性向量空间（Linear Vector Spaces）

> **对应 Shankar**：Chapter 1, Section 1.1
> **前置知识**：[00_预备课_复数.md](00_预备课_复数.md)

---

## 一、从你熟悉的箭头向量出发

高中学过向量。最直观的图像是这样的：

```
  ^ y
  |
  |      B (3, 2)
  |     /
  |    /
  |   /  向量 v
  |  /
  | /
  |/
--+-----------> x
```

向量 $\vec{v} = (3, 2)$ 的意思是：从原点出发，向右走 3，向上走 2。

这种箭头向量有两个核心操作：

### 操作一：向量加法

$$\vec{v} = (3, 2), \quad \vec{w} = (1, -1)$$

$$\vec{v} + \vec{w} = (3+1,\ 2+(-1)) = (4, 1)$$

几何意义：把两个箭头首尾相接。

```
  ^
  |  /v+w
  | /
  |/___/w
  /v
 /
+----------->
```

### 操作二：数乘（标量乘法）

$$2 \cdot \vec{v} = 2 \cdot (3, 2) = (6, 4)$$

几何意义：把箭头拉长 2 倍。如果乘以负数，箭头方向反转。

---

## 二、一个关键问题：什么东西和箭头向量"一样"？

现在想想：向量加法和数乘这两个操作，**本质上满足哪些规律**？

比如：
- 先加 $\vec{w}$ 再加 $\vec{v}$，和先加 $\vec{v}$ 再加 $\vec{w}$，结果一样吗？**一样**（加法交换律）
- $2(\vec{v} + \vec{w})$ 和 $2\vec{v} + 2\vec{w}$ 一样吗？**一样**（分配律）

这些规律对箭头向量成立，因为加法就是分量相加，乘法就是分量乘标量，都满足普通数的运算律。

但关键问题是：**有没有其他东西，虽然长得不像箭头，但加法和数乘也满足同样的规律？**

答案是：**有！而且很多。** 比如——

**多项式**：$f(x) = 1 + 2x + 3x^2$，两个多项式可以相加，可以乘以一个数。

**函数**：$f(x) = \sin x$，两个函数可以相加，可以乘以一个数。

**矩阵**：$\begin{pmatrix}1&2\\3&4\end{pmatrix}$，两个矩阵可以相加，可以乘以一个数。

这些东西和箭头向量都有"共同点"：它们都支持加法和数乘，而且这两个操作都满足一批相同的规律。

**数学家的做法**：把这些共同规律提炼出来，抽象成一个定义——这就是**线性向量空间**。

> 抽象的好处：只要证明一次（在抽象层面），所有满足定义的东西都自动继承这个结论。这比对每种具体对象分别证明效率高得多。

---

## 三、线性向量空间的定义

一个**线性向量空间**（linear vector space，简称向量空间）$V$ 是：

- 一个**集合**（里面的元素叫**向量**）
- 配备**两种操作**：
  - **加法**：两个向量相加得到一个向量
  - **数乘**：一个标量乘以一个向量得到一个向量
- 满足以下 **8 条公理**（axioms）

在量子力学中，标量是**复数**（这就是为什么我们先学复数）。

### 关于符号：狄拉克记法（Dirac Notation）

从现在起，我们用 $\vert a \rangle$ 表示一个向量，读作 "ket a"（右矢 a）。这是量子力学专用的记法，Shankar 从第一章开始就用它。

**暂时理解**：$\vert a \rangle$ 就是"名字叫 $a$ 的向量"。后面我们会看到这个记号更深的含义。

### 8 条公理

设 $\vert a \rangle$，$\vert b \rangle$，$\vert c \rangle$ 是 $V$ 中的向量，$\alpha$，$\beta$ 是复数（标量）。

**关于加法的 4 条：**

| 公理 | 内容 | 说明 |
|------|------|------|
| A1 | $\vert a\rangle + \vert b\rangle = \vert b\rangle + \vert a\rangle$ | 加法交换律 |
| A2 | $(\vert a\rangle + \vert b\rangle) + \vert c\rangle = \vert a\rangle + (\vert b\rangle + \vert c\rangle)$ | 加法结合律 |
| A3 | 存在零向量 $\vert 0\rangle$ 使得 $\vert a\rangle + \vert 0\rangle = \vert a\rangle$ | 加法单位元 |
| A4 | 对每个 $\vert a\rangle$，存在 $\vert{-a}\rangle$ 使得 $\vert a\rangle + \vert{-a}\rangle = \vert 0\rangle$ | 加法逆元 |

**关于数乘的 4 条：**

| 公理 | 内容 | 说明 |
|------|------|------|
| S1 | $\alpha(\vert a\rangle + \vert b\rangle) = \alpha\vert a\rangle + \alpha\vert b\rangle$ | 数乘对向量加法分配 |
| S2 | $(\alpha + \beta)\vert a\rangle = \alpha\vert a\rangle + \beta\vert a\rangle$ | 数乘对标量加法分配 |
| S3 | $\alpha(\beta\vert a\rangle) = (\alpha\beta)\vert a\rangle$ | 数乘结合律 |
| S4 | $1 \cdot \vert a\rangle = \vert a\rangle$ | 数乘单位元 |

### 这些公理在说什么？

这 8 条其实都是你非常熟悉的东西——它们就是普通加法和乘法的基本性质。

**A1（交换律）**：$3 + 5 = 5 + 3$。向量加法也一样。

**A2（结合律）**：$(1+2)+3 = 1+(2+3)$。先加哪两个结果都一样。

**A3（零向量）**：就像 $0$，加上去不改变结果。对箭头向量，零向量是 $(0, 0)$。

**A4（负向量）**：就像 $5 + (-5) = 0$。负向量方向相反、大小相同。

**S1（分配律一）**：$2(3+5) = 2\times3 + 2\times5$。数乘分配进加法。

**S2（分配律二）**：$(2+3)\times5 = 2\times5 + 3\times5$。两个标量之和乘以向量。

**S3（结合律）**：先乘 $\beta$ 再乘 $\alpha$，等于直接乘 $\alpha\beta$。

**S4（单位元）**：乘以 1 不改变向量。

---

## 四、封闭性：容易忽略的要点

在上面的定义里，有一个隐含但极重要的要求：

> **封闭性**（closure）：两个向量相加，结果还在 $V$ 里；一个向量乘以标量，结果还在 $V$ 里。

这看起来理所当然，但对于某些集合会失败。

**反例**：考虑"所有正实数"这个集合，用普通加法和乘法。

- 加法：$3 + 5 = 8$，还是正实数 ✓
- 但数乘：$(-1) \times 3 = -3$，不是正实数 ✗

所以正实数不是向量空间，因为数乘不封闭（乘以负标量会跑出集合）。

---

## 五、三个向量空间的例子

### 例子 1：$\mathbb{R}^2$（平面实向量，你最熟悉的）

**集合**：所有有序实数对 $(x, y)$，$x, y \in \mathbb{R}$

**加法**：$(x_1, y_1) + (x_2, y_2) = (x_1+x_2,\ y_1+y_2)$

**数乘**：$\alpha(x, y) = (\alpha x,\ \alpha y)$

验证 8 条公理都满足（留给你自己检查）。零向量是 $(0, 0)$，负向量是 $(-x, -y)$。

**维度**（后面解释）：2

### 例子 2：次数不超过 $n$ 的多项式

**集合**：$p(x) = a_0 + a_1 x + a_2 x^2 + \cdots + a_n x^n$，系数为实数

**加法**：逐项相加，$(p+q)(x) = p(x) + q(x)$

**数乘**：$(\alpha p)(x) = \alpha \cdot p(x)$

多项式是向量！零向量是零多项式（所有系数为 0）。

**维度**：$n+1$（基是 $1, x, x^2, \ldots, x^n$）

### 例子 3：复值函数（量子力学最重要！）

**集合**：所有函数 $\psi: \mathbb{R} \to \mathbb{C}$（从实数到复数的函数）

**加法**：$(\psi_1 + \psi_2)(x) = \psi_1(x) + \psi_2(x)$（每个点上的函数值相加）

**数乘**：$(\alpha \psi)(x) = \alpha \cdot \psi(x)$

函数是向量！这个空间是**无穷维**的。

> 在量子力学里，粒子的状态用一个函数 $\psi(x)$ 来描述（叫**波函数**）。这个函数就是这个空间里的一个向量（ket）。这就是为什么要学向量空间。

---

## 六、简单推论

从 8 条公理出发，可以推出一些有用的结论：

**推论 1**：$0 \cdot \vert a \rangle = \vert 0 \rangle$（标量 0 乘以任意向量得零向量）

推导：$0 \cdot \vert a\rangle = (0 + 0)\vert a\rangle = 0\vert a\rangle + 0\vert a\rangle$，两边减去 $0\vert a\rangle$ 得 $\vert 0\rangle = 0\vert a\rangle$。

**推论 2**：$\alpha \cdot \vert 0 \rangle = \vert 0 \rangle$（任意标量乘以零向量得零向量）

**推论 3**：$(-1)\vert a\rangle = \vert{-a}\rangle$（乘以 $-1$ 得负向量）

---

## 七、线性组合（Linear Combination）

### 定义

给定向量空间 $V$ 中的向量 $\vert v_1\rangle, \vert v_2\rangle, \ldots, \vert v_n\rangle$ 和复数 $\alpha_1, \alpha_2, \ldots, \alpha_n$，则

$$\alpha_1 \vert v_1\rangle + \alpha_2 \vert v_2\rangle + \cdots + \alpha_n \vert v_n\rangle$$

叫做这组向量的一个**线性组合**（linear combination）。

### 例子

在 $\mathbb{R}^2$ 中，设 $\vert e_1\rangle = (1, 0)$，$\vert e_2\rangle = (0, 1)$。

任意向量 $(3, 5)$ 都是它们的线性组合：

$$(3, 5) = 3 \cdot (1, 0) + 5 \cdot (0, 1) = 3\vert e_1\rangle + 5\vert e_2\rangle$$

在多项式空间中，$3 + 5x^2$ 是 $\{1, x, x^2\}$ 的线性组合：

$$3 + 5x^2 = 3 \cdot 1 + 0 \cdot x + 5 \cdot x^2$$

### 张成集（Span）

一组向量 $\{\vert v_1\rangle, \ldots, \vert v_n\rangle\}$ 的所有线性组合构成的集合，叫做这组向量**张成**（span）的子集，记作 $\text{span}\{\vert v_1\rangle, \ldots, \vert v_n\rangle\}$。

**例子**：在 $\mathbb{R}^2$ 中，$\text{span}\{(1,0)\}$ 是 $x$ 轴（所有 $(\alpha, 0)$ 形式的点）。$\text{span}\{(1,0),(0,1)\}$ 是整个 $\mathbb{R}^2$。

---

## 八、线性无关（Linear Independence）

### 核心问题

一组向量里，有没有"多余的"？某个向量能不能由其他向量表示？

### 定义

一组向量 $\vert v_1\rangle, \vert v_2\rangle, \ldots, \vert v_n\rangle$ **线性无关**（linearly independent），当且仅当：

$$\alpha_1 \vert v_1\rangle + \alpha_2 \vert v_2\rangle + \cdots + \alpha_n \vert v_n\rangle = \vert 0\rangle$$

**蕴含** $\alpha_1 = \alpha_2 = \cdots = \alpha_n = 0$。

换句话说：**唯一能让这个线性组合等于零向量的方式，就是所有系数都是零。**

如果存在一组**不全为零**的系数使线性组合等于零，就叫**线性相关**（linearly dependent）。

### 直觉：线性相关 = 有冗余

如果 $\vert v_1\rangle, \ldots, \vert v_n\rangle$ 线性相关，那么至少有一个向量可以由其他向量表示（是"多余的"）。

**为什么？** 若 $\alpha_1 \vert v_1\rangle + \cdots + \alpha_n \vert v_n\rangle = \vert 0\rangle$，且某个系数，比如 $\alpha_1 \neq 0$，则：

$$\vert v_1\rangle = -\frac{\alpha_2}{\alpha_1}\vert v_2\rangle - \cdots - \frac{\alpha_n}{\alpha_1}\vert v_n\rangle$$

$\vert v_1\rangle$ 被其他向量表示了，它是冗余的。

### 例子 1：$\mathbb{R}^2$ 中的两个向量

**问**：$(1, 0)$ 和 $(0, 1)$ 线性无关吗？

设 $\alpha(1,0) + \beta(0,1) = (0,0)$，即 $(\alpha, \beta) = (0, 0)$，所以 $\alpha = 0$ 且 $\beta = 0$。

**结论**：线性无关。✓ 没有冗余，这两个向量指向完全不同的方向。

### 例子 2：$\mathbb{R}^2$ 中的三个向量

**问**：$(1, 0)$，$(0, 1)$，$(2, 3)$ 线性无关吗？

注意：$2 \cdot (1,0) + 3 \cdot (0,1) - 1 \cdot (2,3) = (2,0) + (0,3) - (2,3) = (0,0)$

这里 $\alpha_1=2, \alpha_2=3, \alpha_3=-1$，不全为零，但组合等于零。

**结论**：线性相关。$(2,3)$ 是冗余的，因为 $(2,3) = 2(1,0) + 3(0,1)$。

### 例子 3：几何直觉

在 $\mathbb{R}^2$ 里，两个向量线性相关，意味着它们**共线**（方向相同或相反）。

```
线性相关（共线）        线性无关（不共线）
      ^                       ^
      | v                     | v
      |                       |
      | u（与 v 同向）         +-----> u
      |
      +-->
```

如果 $\vec{u}$ 和 $\vec{v}$ 共线，那么 $\vec{v} = \alpha \vec{u}$，即 $\alpha \vec{u} - \vec{v} = 0$，系数不全为零，线性相关。

---

## 九、基（Basis）与维度（Dimension）

### 基的定义

向量空间 $V$ 的一组**基**（basis）是满足以下**两个条件**的向量集合 $\{\vert e_1\rangle, \ldots, \vert e_n\rangle\}$：

1. **线性无关**：没有冗余
2. **张成** $V$：$V$ 中任意向量都能表示成它们的线性组合

满足这两个条件，就叫做这组向量**构成 $V$ 的一组基**。

### 维度的定义

$V$ 的**维度**（dimension）是基中向量的个数，记作 $\dim(V)$。

**重要事实**：同一个向量空间，任何一组基都有相同数量的向量。（这不是显然的，但可以证明。）所以维度是向量空间本身的属性，不依赖于选哪组基。

### 例子

$\mathbb{R}^2$ 的**标准基**：$\vert e_1\rangle = (1,0)$，$\vert e_2\rangle = (0,1)$

验证：
- 线性无关：✓（前面验证了）
- 张成 $\mathbb{R}^2$：✓（任意 $(x,y) = x(1,0) + y(0,1)$）

所以 $\dim(\mathbb{R}^2) = 2$。

### 基不唯一

$\mathbb{R}^2$ 的另一组合法的基：$\vert f_1\rangle = (1,1)$，$\vert f_2\rangle = (1,-1)$

验证线性无关：设 $\alpha(1,1) + \beta(1,-1) = (0,0)$，即 $\alpha+\beta=0$ 且 $\alpha-\beta=0$，解得 $\alpha=\beta=0$。✓

验证张成 $\mathbb{R}^2$：任意 $(x,y)$，令 $\alpha(1,1)+\beta(1,-1) = (x,y)$，即 $\alpha+\beta=x$，$\alpha-\beta=y$，解得 $\alpha=(x+y)/2$，$\beta=(x-y)/2$。✓

所以这也是一组基，但维度仍然是 2。

### 坐标（Coordinates）

在基 $\{\vert e_i\rangle\}$ 下，向量 $\vert v\rangle = \sum_i c_i \vert e_i\rangle$ 中的系数 $c_i$ 叫做 $\vert v\rangle$ 在这组基下的**坐标**。

不同的基下，同一个向量的坐标不同——但向量本身不变。这就像同一个城市，用经纬度和用地图方格都可以定位，坐标不同，城市还是那个城市。

---

## 十、本节总结

```
线性向量空间
│
├── 集合 + 加法 + 数乘（满足 8 条公理）
│
├── 元素叫"向量"
│     ├── 箭头是向量
│     ├── 多项式是向量
│     └── 函数是向量（量子力学核心！）
│
├── 线性组合：α₁|v₁⟩ + α₂|v₂⟩ + ...
│
├── 线性无关：无冗余
│     └── 判断方式：令线性组合 = 零向量，看系数是否全为零
│
├── 基：线性无关 + 张成整个空间
│
└── 维度：基中向量的个数（与基的选择无关）
```

量子力学的状态空间是**无穷维**的复数函数空间，但这里的所有概念都适用。

---

## 小测试

1. 验证：$\mathbb{R}^3$ 中的标准基 $(1,0,0)$，$(0,1,0)$，$(0,0,1)$ 是线性无关的。

2. 在 $\mathbb{R}^3$ 中，判断 $(1,2,3)$，$(2,4,6)$ 是否线性相关，并说明原因。

3. 以下集合是不是向量空间？说明理由。
   - a) 所有满足 $x + y = 1$ 的点 $(x,y)$（普通加法和数乘）
   - b) 所有 $2\times 2$ 实数矩阵（矩阵加法和数乘）

4. 多项式 $p(x) = 2 + 3x - x^2$ 在基 $\{1, x, x^2\}$ 下的坐标是什么？

<details>
<summary>点击查看答案</summary>

1. 设 $\alpha(1,0,0) + \beta(0,1,0) + \gamma(0,0,1) = (0,0,0)$，则 $(\alpha,\beta,\gamma) = (0,0,0)$，所以 $\alpha=\beta=\gamma=0$。线性无关。✓

2. $(2,4,6) = 2 \cdot (1,2,3)$，即 $2(1,2,3) - 1(2,4,6) = (0,0,0)$，系数不全为零，所以**线性相关**。直觉：两个向量共线（一个是另一个的倍数）。

3. a) **不是向量空间**：取 $(1,0)$ 和 $(0,1)$，都满足 $x+y=1$，但它们的和 $(1,1)$ 满足 $1+1=2\neq 1$，加法不封闭。另外零向量 $(0,0)$ 满足 $0+0=0\neq 1$，也不在集合里。
   b) **是向量空间**：矩阵加法和数乘满足全部 8 条公理。

4. $p(x) = 2 \cdot 1 + 3 \cdot x + (-1) \cdot x^2$，坐标是 $(2, 3, -1)$。

</details>

---

下一节：[02_第一章_内积空间.md](02_第一章_内积空间.md)

引入向量的"长度"和"角度"——内积空间。
