# 第一章第四、五节：子空间 & 线性算符

> 对应 Shankar Chapter 1, Sections 1.4–1.5
> 前置知识：[03_第一章_对偶空间与狄拉克符号.md](03_第一章_对偶空间与狄拉克符号.md)

---

## 1.4 子空间（Subspace）

**定义**：$V$ 的非空子集 $W$ 叫**子空间**，若它本身也是向量空间。

只需验证 3 条（其余从 $V$ 继承）：
1. $\vert 0\rangle\in W$
2. $\vert a\rangle,\vert b\rangle\in W\implies\vert a\rangle+\vert b\rangle\in W$
3. $\vert a\rangle\in W,\alpha\in\mathbb{C}\implies\alpha\vert a\rangle\in W$

**例**：$\mathbb{R}^2$ 里过原点的直线 $\{(t,2t)\}$ 是子空间；不过原点的直线不是（零向量不在里面）。

---

## 1.5 线性算符

### 定义

**线性算符** $\hat{A}:V\to V$ 把向量映射到向量，满足：

$$\hat{A}(\alpha\vert a\rangle+\beta\vert b\rangle) = \alpha\hat{A}\vert a\rangle+\beta\hat{A}\vert b\rangle$$

### 例子

- **外积** $\vert u\rangle\langle v\vert$：作用结果 $(\vert u\rangle\langle v\vert)\vert a\rangle=\langle v\vert a\rangle\cdot\vert u\rangle$
- **矩阵**：$n\times n$ 矩阵乘以列向量，是线性算符
- **恒等算符** $\hat{I}$：$\hat{I}\vert a\rangle=\vert a\rangle$
- **零算符** $\hat{0}$：$\hat{0}\vert a\rangle=\vert 0\rangle$

### 矩阵表示

在正交归一基 $\{\vert e_i\rangle\}$ 下，算符 $\hat{A}$ 的矩阵元为：

$$\boxed{A_{ij} = \langle e_i\vert\hat{A}\vert e_j\rangle}$$

**例**：$\mathbb{R}^2$ 逆时针旋转 $90°$：$R\vert e_1\rangle=\vert e_2\rangle$，$R\vert e_2\rangle=-\vert e_1\rangle$

$$R=\begin{pmatrix}0&-1\\1&0\end{pmatrix}$$

### 算符乘法（复合）

$$(\hat{A}\hat{B})\vert v\rangle = \hat{A}(\hat{B}\vert v\rangle)$$

先作用 $\hat{B}$，再作用 $\hat{A}$。

⚠️ **一般 $\hat{A}\hat{B}\neq\hat{B}\hat{A}$**（不对易）——这是量子力学不确定性原理的数学根源。

### 厄米共轭 $\hat{A}^\dagger$

定义：$\langle a\vert\hat{A}^\dagger\vert b\rangle = \langle b\vert\hat{A}\vert a\rangle^*$

矩阵操作：转置 + 每个元素取复共轭，$(A^\dagger)_{ij}=A_{ji}^*$

---

## 两类重要算符

### 厄米算符（Hermitian）：$\hat{A}^\dagger=\hat{A}$

矩阵满足 $M^\dagger=M$。

**量子力学意义**：每个可测量的物理量（位置、动量、能量）都是厄米算符。原因：厄米算符的本征值是实数，测量结果必须是实数。

**例**：$\begin{pmatrix}1&2-i\\2+i&3\end{pmatrix}$ 是厄米矩阵（验证：转置共轭等于自身）。

### 幺正算符（Unitary）：$\hat{U}^\dagger\hat{U}=\hat{I}$

即 $\hat{U}^\dagger=\hat{U}^{-1}$。

**量子力学意义**：量子态的时间演化由幺正算符描述，因为幺正算符保持向量范数：

$$\Vert\hat{U}\vert a\rangle\Vert^2=\langle a\vert\hat{U}^\dagger\hat{U}\vert a\rangle=\langle a\vert a\rangle$$

（总概率守恒）

**例**：旋转矩阵 $R=\begin{pmatrix}0&-1\\1&0\end{pmatrix}$，$R^T R=I$，是幺正算符。

---

## 总结

```
线性算符
├── 把向量映射到向量，线性
├── 矩阵表示：A_ij = ⟨eᵢ|Â|eⱼ⟩
├── 乘法不对易：AB ≠ BA（→ 不确定性原理）
├── 厄米共轭：矩阵取转置复共轭
├── 厄米算符 Â†=Â → 本征值实数 → 可观测量
└── 幺正算符 Û†Û=I → 保范数 → 时间演化
```

---

## 练习

$\hat{A}\vert e_1\rangle=\vert e_1\rangle+\vert e_2\rangle$，$\hat{A}\vert e_2\rangle=\vert e_1\rangle-\vert e_2\rangle$

1. 求 $\hat{A}$ 的矩阵
2. 求 $\hat{A}^\dagger$
3. $\hat{A}$ 是厄米算符吗？是幺正算符吗？

<details>
<summary>答案</summary>

1. $\hat{A}=\begin{pmatrix}1&1\\1&-1\end{pmatrix}$

2. $\hat{A}^\dagger=\begin{pmatrix}1&1\\1&-1\end{pmatrix}$（实数矩阵，转置等于自身）

3. $\hat{A}^\dagger=\hat{A}$，**是厄米算符**；$\hat{A}^2=2I\neq I$，**不是幺正算符**（$\frac{1}{\sqrt{2}}\hat{A}$ 才是）

</details>

---

下一节：[05_第一章_本征值问题.md](05_第一章_本征值问题.md)
