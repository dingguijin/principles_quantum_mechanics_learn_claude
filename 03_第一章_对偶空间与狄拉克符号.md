# 第一章第三节：对偶空间与狄拉克符号（Dual Spaces and Dirac Notation）

> 对应 Shankar Chapter 1, Section 1.3
> 前置知识：[02_第一章_内积空间.md](02_第一章_内积空间.md)

---

## $\langle a\vert$ 到底是什么？

它是一个**把向量线性地映射到复数的函数**：

$$\langle a\vert:\quad \vert b\rangle \mapsto \langle a\vert b\rangle\in\mathbb{C}$$

线性：$\langle a\vert(\alpha\vert b\rangle+\beta\vert c\rangle) = \alpha\langle a\vert b\rangle+\beta\langle a\vert c\rangle$（由内积规则 2）

---

## 线性泛函

**定义**：$f:V\to\mathbb{C}$ 满足 $f(\alpha\vert a\rangle+\beta\vert b\rangle)=\alpha f(\vert a\rangle)+\beta f(\vert b\rangle)$，叫**线性泛函**。

**例**：固定 $\vert u\rangle$，定义 $f(\vert v\rangle)=\langle u\vert v\rangle$——这就是 bra $\langle u\vert$。

---

## 对偶空间 $V^*$

所有 $V$ 上的线性泛函构成一个向量空间，叫 $V$ 的**对偶空间** $V^*$。

- $V$ 里住 ket：$\vert a\rangle$
- $V^*$ 里住 bra：$\langle a\vert$

**重要事实**：对有限维内积空间，$V$ 和 $V^*$ 之间有一一对应——每个 ket 对应唯一一个 bra（反之亦然）。

---

## Ket → Bra：系数取复共轭

设 $\vert a\rangle=\sum_i c_i\vert e_i\rangle$（正交归一基展开），则对应的 bra：

$$\langle a\vert = \sum_i c_i^*\langle e_i\vert$$

**必须取共轭的原因**：这是保证 $\langle a\vert a\rangle = \sum_i\vert c_i\vert^2\geq 0$ 的唯一方式。

若不取共轭，$\langle a\vert a\rangle=\sum_i c_i^2$ 可以是负数或复数。

---

## 矩阵表示

$$\vert a\rangle=\begin{pmatrix}a_1\\\vdots\\a_n\end{pmatrix}\quad\longleftrightarrow\quad\langle a\vert=\begin{pmatrix}a_1^*&\cdots&a_n^*\end{pmatrix}$$

操作名：**厄米共轭**（Hermitian conjugate），记作 $\dagger$（"dagger"）：$\langle a\vert=(\vert a\rangle)^\dagger$

**内积** = 行 $\times$ 列 = 数；**外积** = 列 $\times$ 行 = 矩阵

---

## 外积 $\vert a\rangle\langle b\vert$

$$\vert a\rangle\langle b\vert=\begin{pmatrix}a_1\\a_2\end{pmatrix}\begin{pmatrix}b_1^*&b_2^*\end{pmatrix}=\begin{pmatrix}a_1b_1^*&a_1b_2^*\\a_2b_1^*&a_2b_2^*\end{pmatrix}$$

作用在 ket 上：$(\vert a\rangle\langle b\vert)\vert c\rangle = \langle b\vert c\rangle\cdot\vert a\rangle$

先算内积 $\langle b\vert c\rangle$（得到数），再乘以 $\vert a\rangle$。

---

## 完备性关系 ⭐⭐⭐

设 $\{\vert e_i\rangle\}$ 是正交归一基，对任意 $\vert v\rangle=\sum_i c_i\vert e_i\rangle$（$c_i=\langle e_i\vert v\rangle$）：

$$\vert v\rangle=\sum_i c_i\vert e_i\rangle=\sum_i\langle e_i\vert v\rangle\cdot\vert e_i\rangle=\sum_i\vert e_i\rangle\langle e_i\vert v\rangle=\left(\sum_i\vert e_i\rangle\langle e_i\vert\right)\vert v\rangle$$

因为对任意 $\vert v\rangle$ 都成立：

$$\boxed{\sum_i\vert e_i\rangle\langle e_i\vert=\hat{I}}$$

验证（标准基）：$\begin{pmatrix}1\\0\end{pmatrix}\begin{pmatrix}1&0\end{pmatrix}+\begin{pmatrix}0\\1\end{pmatrix}\begin{pmatrix}0&1\end{pmatrix}=\begin{pmatrix}1&0\\0&0\end{pmatrix}+\begin{pmatrix}0&0\\0&1\end{pmatrix}=I$ ✓

---

## 完整的 Dirac 符号体系

| 符号 | 是什么 | 数学类型 |
|------|--------|---------|
| $\vert a\rangle$ | ket，量子态 | 列向量，$\in V$ |
| $\langle a\vert$ | bra，线性泛函 | 行向量（共轭），$\in V^*$ |
| $\langle a\vert b\rangle$ | 内积 | 复数 |
| $\vert a\rangle\langle b\vert$ | 外积 | 矩阵/算符 |
| $\sum_i\vert e_i\rangle\langle e_i\vert$ | 完备性 | $=\hat{I}$ |

---

## 练习

$\vert a\rangle=(1+i,2)^T$，$\vert v\rangle=(1,i)^T$，$\vert e_1\rangle=\frac{1}{\sqrt{2}}(1,1)^T$，$\vert e_2\rangle=\frac{1}{\sqrt{2}}(1,-1)^T$

1. 写出 $\langle a\vert$
2. 计算外积 $\vert a\rangle\langle a\vert$（$2\times 2$ 矩阵）
3. 用矩阵法和公式法两种方式算 $(\vert a\rangle\langle a\vert)\vert v\rangle$，验证一致
4. 验证 $\vert e_1\rangle\langle e_1\vert+\vert e_2\rangle\langle e_2\vert=I$

<details>
<summary>答案</summary>

1. $\langle a\vert=(1-i,\ 2)$

2. $\vert a\rangle\langle a\vert=\begin{pmatrix}2&2+2i\\2-2i&4\end{pmatrix}$

3. 矩阵法得 $(2i,\ 2+2i)^T$；公式法：$\langle a\vert v\rangle=1+i$，$(1+i)(1+i,2)^T=(2i,2+2i)^T$ ✓

4. $\frac{1}{2}\begin{pmatrix}1&1\\1&1\end{pmatrix}+\frac{1}{2}\begin{pmatrix}1&-1\\-1&1\end{pmatrix}=\begin{pmatrix}1&0\\0&1\end{pmatrix}=I$ ✓

</details>

---

下一节：[04_第一章_线性算符.md](04_第一章_线性算符.md)
