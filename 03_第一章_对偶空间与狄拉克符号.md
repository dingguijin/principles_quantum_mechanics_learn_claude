# 第一章第三节：对偶空间与狄拉克符号（Dual Spaces and Dirac Notation）

> **对应 Shankar**：Chapter 1, Section 1.3
> **前置知识**：[02_第一章_内积空间.md](02_第一章_内积空间.md)

---

## 一、bra 到底是什么？

上一节我们说 $\langle a\vert$ 是"接收器"，吃进一个 ket，吐出一个复数。但这个说法还很粗糙。

这一节我们把它说精确：**bra 是一种特殊的函数（叫线性泛函），住在 ket 空间的"对偶空间"里。**

---

## 二、函数的函数：线性泛函

你知道函数是什么：把一个数映射到另一个数，比如 $f(x) = x^2$。

现在考虑一种不同的函数：**输入是一个向量，输出是一个（复）数**。

$$f: V \to \mathbb{C}$$

这样的函数叫做 $V$ 上的**泛函**（functional）。"泛函"就是"吃向量的函数"。

如果这个泛函还是**线性**的（即满足叠加原理）：

$$f\bigl(\alpha\vert a\rangle + \beta\vert b\rangle\bigr) = \alpha\, f(\vert a\rangle) + \beta\, f(\vert b\rangle)$$

就叫做**线性泛函**（linear functional）。

### 例子 1：固定一个 ket，用内积定义线性泛函

固定向量 $\vert u\rangle \in V$，定义：

$$f_u(\vert v\rangle) = \langle u\vert v\rangle$$

这就是一个线性泛函——对任意输入 $\vert v\rangle$，输出内积值 $\langle u\vert v\rangle \in \mathbb{C}$。

验证线性：

$$f_u(\alpha\vert a\rangle + \beta\vert b\rangle) = \langle u\vert\alpha\vert a\rangle + \beta\vert b\rangle\rangle = \alpha\langle u\vert a\rangle + \beta\langle u\vert b\rangle = \alpha f_u(\vert a\rangle) + \beta f_u(\vert b\rangle) \checkmark$$

（用了内积规则 2：第二参数线性。）

**关键认识：$\langle u\vert$ 就是线性泛函 $f_u$！**

### 例子 2：在 $\mathbb{R}^2$ 中

固定 $\vec{u} = (u_1, u_2)$，泛函 $f_u(\vec{v}) = \vec{u}\cdot\vec{v} = u_1 v_1 + u_2 v_2$。

这是一个线性函数：输入一个向量，输出一个数。这就是 $\langle u\vert$ 在 $\mathbb{R}^2$ 中的具体形式。

---

## 三、对偶空间（Dual Space）

所有 $V$ 上的线性泛函本身也构成一个向量空间（可以把两个线性泛函相加，也可以乘以标量），这个空间叫做 $V$ 的**对偶空间**（dual space），记作 $V^*$。

$$V^* = \{f: V\to\mathbb{C} \mid f \text{ 是线性的}\}$$

所以：
- $V$（ket 空间）里住的是 ket：$\vert a\rangle$，$\vert b\rangle$，...
- $V^*$（bra 空间 / 对偶空间）里住的是 bra：$\langle a\vert$，$\langle b\vert$，...

### Riesz 表示定理

这是一个深刻的数学定理，它说：

> 对于（有限维的）内积空间，**每一个**线性泛函 $f \in V^*$ 都唯一对应 $V$ 里的某个向量 $\vert u\rangle$，使得：
> $$f(\vert v\rangle) = \langle u\vert v\rangle \quad \text{对所有 } \vert v\rangle \in V$$

**白话翻译**：$V^*$ 里的所有线性泛函，都可以写成某个 bra $\langle u\vert$ 的形式。$V$ 和 $V^*$ 之间有一一对应。

这就是为什么我们可以放心地用 $\langle a\vert$ 代表任意线性泛函——它们都是某个 ket 对应的 bra。

---

## 四、Ket 与 Bra 的精确对应关系

每个 ket $\vert a\rangle$ 对应一个 bra $\langle a\vert$，对应规则是：

$$\vert a\rangle = \sum_i c_i \vert e_i\rangle \quad \longleftrightarrow \quad \langle a\vert = \sum_i c_i^*\langle e_i\vert$$

**系数要取复共轭。**

### 这个对应是"反线性"的

若 $\vert v\rangle = \alpha\vert a\rangle$，则对应的 bra 是：

$$\langle v\vert = \alpha^* \langle a\vert$$

（不是 $\alpha\langle a\vert$，是 $\alpha^*\langle a\vert$！）

**一步步验证**：$\vert v\rangle = \alpha\vert a\rangle = \alpha\sum_i c_i\vert e_i\rangle = \sum_i (\alpha c_i)\vert e_i\rangle$

对应的 bra：$\langle v\vert = \sum_i (\alpha c_i)^*\langle e_i\vert = \sum_i \alpha^* c_i^*\langle e_i\vert = \alpha^*\sum_i c_i^*\langle e_i\vert = \alpha^*\langle a\vert$ ✓

### 为什么必须取复共轭？

这是为了让自内积 $\langle a\vert a\rangle$ 是非负实数：

$$\langle a\vert a\rangle = \left(\sum_i c_i^*\langle e_i\vert\right)\left(\sum_j c_j\vert e_j\rangle\right) = \sum_{i,j} c_i^* c_j \langle e_i\vert e_j\rangle = \sum_{i,j} c_i^* c_j \delta_{ij} = \sum_i c_i^* c_i = \sum_i \vert c_i\vert^2$$

结果是各 $\vert c_i\vert^2$ 之和，一定是非负实数。✓

如果对应是线性的（不取共轭），结果会是 $\sum_i c_i^2$，可以是负数或复数，没有物理意义。

---

## 五、矩阵语言：dagger（$\dagger$）操作

在有限维空间里，用列向量表示 ket，用行向量表示 bra：

$$\vert a\rangle = \begin{pmatrix}a_1\\a_2\\\vdots\\a_n\end{pmatrix} \quad \longleftrightarrow \quad \langle a\vert = \begin{pmatrix}a_1^* & a_2^* & \cdots & a_n^*\end{pmatrix}$$

从 ket（列向量）到 bra（行向量）的操作是：**转置 + 逐元素取复共轭**。

这个操作叫**厄米共轭**（Hermitian conjugate）或 **dagger 操作**，记作 $\dagger$（读 "dagger"）：

$$\langle a\vert = (\vert a\rangle)^\dagger$$

**内积 = 行向量 × 列向量**（标准矩阵乘法）：

$$\langle a\vert b\rangle = \begin{pmatrix}a_1^*&a_2^*\end{pmatrix}\begin{pmatrix}b_1\\b_2\end{pmatrix} = a_1^* b_1 + a_2^* b_2$$

### 具体例子

$$\vert a\rangle = \begin{pmatrix}3\\4i\end{pmatrix} \implies \langle a\vert = \begin{pmatrix}3 & -4i\end{pmatrix}$$

$$\langle a\vert a\rangle = \begin{pmatrix}3&-4i\end{pmatrix}\begin{pmatrix}3\\4i\end{pmatrix} = 9 + (-4i)(4i) = 9 - 16i^2 = 9 + 16 = 25$$

$$\Vert\,\vert a\rangle\,\Vert = \sqrt{25} = 5 \checkmark$$

---

## 六、外积（Outer Product）：$\vert a\rangle\langle b\vert$

我们已经知道**内积** $\langle a\vert b\rangle$ = 行向量 × 列向量 = **数**。

还有一种乘法——**外积**：$\vert a\rangle\langle b\vert$ = 列向量 × 行向量 = **矩阵**。

$$\vert a\rangle\langle b\vert = \begin{pmatrix}a_1\\a_2\end{pmatrix}\begin{pmatrix}b_1^*&b_2^*\end{pmatrix} = \begin{pmatrix}a_1 b_1^* & a_1 b_2^*\\ a_2 b_1^* & a_2 b_2^*\end{pmatrix}$$

外积 $\vert a\rangle\langle b\vert$ 是一个**算符**（operator）——它可以作用在任意 ket 上：

$$\bigl(\vert a\rangle\langle b\vert\bigr)\vert c\rangle = \vert a\rangle\bigl(\langle b\vert c\rangle\bigr) = \langle b\vert c\rangle\cdot\vert a\rangle$$

先用 $\langle b\vert$ 和 $\vert c\rangle$ 计算内积（得到一个数），再乘以 $\vert a\rangle$。

**理解方式**：$\vert a\rangle\langle b\vert$ 是一个"投影-缩放"机器，把任意向量投影到 $\vert b\rangle$ 方向，然后输出 $\vert a\rangle$ 方向的向量。

---

## 七、完备性关系（Completeness Relation）⭐⭐⭐

这是 Dirac 符号最强大的应用之一。

设 $\{\vert e_1\rangle, \vert e_2\rangle, \ldots, \vert e_n\rangle\}$ 是 $V$ 的正交归一基，则：

$$\boxed{\sum_{i=1}^{n} \vert e_i\rangle\langle e_i\vert = \hat{I}}$$

其中 $\hat{I}$ 是**恒等算符**（identity operator），作用在任何向量上都返回它自身：$\hat{I}\vert v\rangle = \vert v\rangle$。

### 证明

取任意 $\vert v\rangle$，它可以在正交归一基下展开，展开系数 $c_j = \langle e_j\vert v\rangle$：

$$\left(\sum_i \vert e_i\rangle\langle e_i\vert\right)\vert v\rangle = \sum_i \vert e_i\rangle\underbrace{\langle e_i\vert v\rangle}_{c_i} = \sum_i c_i\vert e_i\rangle = \vert v\rangle \checkmark$$

因为对任意 $\vert v\rangle$ 都成立，所以 $\sum_i\vert e_i\rangle\langle e_i\vert = \hat{I}$。

### 验证（$\mathbb{R}^2$）

$\vert e_1\rangle = (1,0)^T$，$\vert e_2\rangle = (0,1)^T$：

$$\vert e_1\rangle\langle e_1\vert + \vert e_2\rangle\langle e_2\vert = \begin{pmatrix}1\\0\end{pmatrix}\begin{pmatrix}1&0\end{pmatrix} + \begin{pmatrix}0\\1\end{pmatrix}\begin{pmatrix}0&1\end{pmatrix} = \begin{pmatrix}1&0\\0&0\end{pmatrix} + \begin{pmatrix}0&0\\0&1\end{pmatrix} = \begin{pmatrix}1&0\\0&1\end{pmatrix} = I \checkmark$$

### 完备性关系的用法

完备性关系最重要的用法是"**在表达式中间插入恒等算符**"来化简计算，比如：

$$\langle a\vert b\rangle = \langle a\vert\hat{I}\vert b\rangle = \langle a\vert\left(\sum_i\vert e_i\rangle\langle e_i\vert\right)\vert b\rangle = \sum_i \langle a\vert e_i\rangle\langle e_i\vert b\rangle$$

这把内积分解成了各基向量分量的乘积之和——非常有用。

---

## 八、Dirac 符号完整体系总结

| 符号 | 名称 | 数学类型 | 说明 |
|------|------|------|------|
| $\vert a\rangle$ | ket（右矢） | 向量 $\in V$ | 量子态 |
| $\langle a\vert$ | bra（左矢） | 线性泛函 $\in V^*$ | ket 的对偶，系数取共轭 |
| $\langle a\vert b\rangle$ | 内积 | 复数 | 两态的"重叠" |
| $\vert a\rangle\langle b\vert$ | 外积 | 算符（矩阵） | 将向量映射到向量 |
| $\sum_i\vert e_i\rangle\langle e_i\vert$ | 完备性关系 | $= \hat{I}$ | 正交归一基张成整个空间 |

**记忆技巧**：

- bra-ket 合起来 = **bracket**（括号）
- 内积 $\langle a\vert b\rangle$：bra 在左，ket 在右，结果是数
- 外积 $\vert a\rangle\langle b\vert$：ket 在左，bra 在右，结果是算符（矩阵）

---

## 九、一个综合例子

设 $\mathbb{C}^2$ 的正交归一基为 $\vert+\rangle = \dfrac{1}{\sqrt{2}}\begin{pmatrix}1\\1\end{pmatrix}$，$\vert-\rangle = \dfrac{1}{\sqrt{2}}\begin{pmatrix}1\\-1\end{pmatrix}$

**第一步**：验证是正交归一基

$$\langle+\vert+\rangle = \frac{1}{\sqrt{2}}\frac{1}{\sqrt{2}}(1^*\cdot 1 + 1^*\cdot 1) = \frac{1}{2}(1+1) = 1 \checkmark$$

$$\langle-\vert-\rangle = \frac{1}{2}(1+1) = 1 \checkmark$$

$$\langle+\vert-\rangle = \frac{1}{2}(1^*\cdot 1 + 1^*\cdot(-1)) = \frac{1}{2}(1-1) = 0 \checkmark$$

**第二步**：展开 $\vert v\rangle = \begin{pmatrix}3\\1\end{pmatrix}$ 在这组基下

$$c_+ = \langle+\vert v\rangle = \frac{1}{\sqrt{2}}(1^*\cdot 3 + 1^*\cdot 1) = \frac{4}{\sqrt{2}} = 2\sqrt{2}$$

$$c_- = \langle-\vert v\rangle = \frac{1}{\sqrt{2}}(1^*\cdot 3 + (-1)^*\cdot 1) = \frac{2}{\sqrt{2}} = \sqrt{2}$$

**验证**：

$$c_+\vert+\rangle + c_-\vert-\rangle = 2\sqrt{2}\cdot\frac{1}{\sqrt{2}}\begin{pmatrix}1\\1\end{pmatrix} + \sqrt{2}\cdot\frac{1}{\sqrt{2}}\begin{pmatrix}1\\-1\end{pmatrix} = \begin{pmatrix}2\\2\end{pmatrix} + \begin{pmatrix}1\\-1\end{pmatrix} = \begin{pmatrix}3\\1\end{pmatrix} \checkmark$$

---

## 小测试

1. 以下哪个是线性泛函（哪个满足线性要求）？
   - a) $f(\vert v\rangle) = \langle u\vert v\rangle + 1$（固定 $\vert u\rangle$，结果加 1）
   - b) $f(\vert v\rangle) = \langle u\vert v\rangle^2$（内积的平方）
   - c) $f(\vert v\rangle) = \langle u\vert v\rangle$（普通内积）

2. 设 $\vert a\rangle = \begin{pmatrix}2\\i\end{pmatrix}$，写出 $\langle a\vert$，计算外积 $\vert a\rangle\langle a\vert$（写成 $2\times 2$ 矩阵）。

3. 用完备性关系 $\vert e_1\rangle\langle e_1\vert + \vert e_2\rangle\langle e_2\vert = I$ 验证：对 $\vert v\rangle = \begin{pmatrix}3\\4\end{pmatrix}$，确实有 $I\vert v\rangle = \vert v\rangle$。

<details>
<summary>点击查看答案</summary>

1. 只有 **c)** 是线性泛函。
   - a) $f(\alpha\vert v\rangle) = \alpha\langle u\vert v\rangle + 1 \neq \alpha(\langle u\vert v\rangle + 1) = \alpha f(\vert v\rangle)$，不线性。
   - b) $f(\alpha\vert v\rangle) = \alpha^2\langle u\vert v\rangle^2 \neq \alpha\langle u\vert v\rangle^2 = \alpha f(\vert v\rangle)$，不线性。
   - c) 满足线性（上一节验证了）。

2. $\langle a\vert = \begin{pmatrix}2 & -i\end{pmatrix}$

   $$\vert a\rangle\langle a\vert = \begin{pmatrix}2\\i\end{pmatrix}\begin{pmatrix}2&-i\end{pmatrix} = \begin{pmatrix}4&-2i\\2i&1\end{pmatrix}$$

   （注意 $i \cdot (-i) = -i^2 = 1$）

3. $\vert e_1\rangle = (1,0)^T$，$\vert e_2\rangle = (0,1)^T$

   $$(\vert e_1\rangle\langle e_1\vert + \vert e_2\rangle\langle e_2\vert)\begin{pmatrix}3\\4\end{pmatrix} = \begin{pmatrix}1\\0\end{pmatrix}(1\cdot 3) + \begin{pmatrix}0\\1\end{pmatrix}(1\cdot 4) = \begin{pmatrix}3\\0\end{pmatrix}+\begin{pmatrix}0\\4\end{pmatrix} = \begin{pmatrix}3\\4\end{pmatrix} \checkmark$$

</details>

---

下一节：[04_第一章_线性算符.md](04_第一章_线性算符.md)

线性算符——量子力学"可观测量"（位置、动量、能量）的数学语言，本章最重要的部分。
