# 第一章第三节：对偶空间与狄拉克符号（Dual Spaces and Dirac Notation）

> **对应 Shankar**：Chapter 1, Section 1.3  
> **前置知识**：[02_第一章_内积空间.md](02_第一章_内积空间.md)

---

## 线性泛函（Linear Functional）

**定义**：线性泛函是映射 $f: V \to \mathbb{C}$，满足：

$$f\bigl(\alpha|a\rangle + \beta|b\rangle\bigr) = \alpha f(|a\rangle) + \beta f(|b\rangle)$$

**例子**：固定向量 $|a\rangle$，定义 $f_{|a\rangle}(|b\rangle) = \langle a|b\rangle$，这就是一个线性泛函（bra！）。

---

## 对偶空间（Dual Space）$V^*$

所有线性泛函构成的向量空间叫 $V$ 的**对偶空间** $V^*$。

- $V$ 里住 ket：$|a\rangle$
- $V^*$ 里住 bra：$\langle a|$

**Riesz 表示定理**：$V^*$ 里每个线性泛函都唯一对应 $V$ 里的某个向量。即：所有线性泛函都可以写成某个 bra 的形式。

---

## Ket 与 Bra 的对应关系

$$|a\rangle = \sum_i c_i |e_i\rangle \quad \longleftrightarrow \quad \langle a| = \sum_i c_i^* \langle e_i|$$

**系数取复共轭**（反线性对应）。

推论：若 $|v\rangle = \alpha|a\rangle$，则 $\langle v| = \alpha^*\langle a|$（不是 $\alpha\langle a|$）。

**原因**：只有反线性对应，才能保证 $\langle a|a\rangle = \sum_i|c_i|^2 \geq 0$。

---

## 矩阵语言

在 $n$ 维空间：

$$|a\rangle = \begin{pmatrix}a_1\\\vdots\\a_n\end{pmatrix} \quad \longleftrightarrow \quad \langle a| = \begin{pmatrix}a_1^* & \cdots & a_n^*\end{pmatrix}$$

bra = ket 列向量取**转置 + 复共轭**，记作 $\langle a| = (|a\rangle)^\dagger$（$\dagger$ 读 "dagger"，厄米共轭）。

内积 = 行向量 × 列向量：

$$\langle a|b\rangle = \begin{pmatrix}a_1^*&\cdots&a_n^*\end{pmatrix}\begin{pmatrix}b_1\\\vdots\\b_n\end{pmatrix} = \sum_i a_i^* b_i$$

---

## 外积（Outer Product）：$|a\rangle\langle b|$

列向量 × 行向量 = 矩阵（算符）：

$$|a\rangle\langle b| = \begin{pmatrix}a_1\\\vdots\\a_n\end{pmatrix}\begin{pmatrix}b_1^*&\cdots&b_n^*\end{pmatrix}$$

作用在 ket 上：

$$\bigl(|a\rangle\langle b|\bigr)|c\rangle = |a\rangle\underbrace{\langle b|c\rangle}_{\text{复数}} = \langle b|c\rangle\cdot|a\rangle$$

---

## 完备性关系（Completeness Relation）⭐⭐⭐

设 $\{|e_i\rangle\}$ 是正交归一基，则：

$$\boxed{\sum_i |e_i\rangle\langle e_i| = \hat{I}}$$

**证明**：对任意 $|v\rangle = \sum_j c_j|e_j\rangle$，$c_j = \langle e_j|v\rangle$：

$$\sum_i|e_i\rangle\langle e_i|v\rangle = \sum_i c_i|e_i\rangle = |v\rangle \checkmark$$

**例子**（$\mathbb{R}^2$ 标准基）：

$$\begin{pmatrix}1\\0\end{pmatrix}\begin{pmatrix}1&0\end{pmatrix} + \begin{pmatrix}0\\1\end{pmatrix}\begin{pmatrix}0&1\end{pmatrix} = \begin{pmatrix}1&0\\0&0\end{pmatrix}+\begin{pmatrix}0&0\\0&1\end{pmatrix} = \begin{pmatrix}1&0\\0&1\end{pmatrix} = I \checkmark$$

---

## 完整的狄拉克符号表

| 符号 | 名称 | 类型 | 描述 |
|------|------|------|------|
| $\|a\rangle$ | ket（右矢） | 向量 $\in V$ | 量子态 |
| $\langle a\|$ | bra（左矢） | 线性泛函 $\in V^*$ | ket 的对偶，系数取共轭 |
| $\langle a\|b\rangle$ | 内积 | 复数 | 两态的"重叠" |
| $\|a\rangle\langle b\|$ | 外积 | 算符 | 把向量映射到向量 |
| $\sum_i\|e_i\rangle\langle e_i\|$ | 完备性关系 | $= \hat{I}$ | 正交归一基的覆盖 |

---

## 小测试

$|e_1\rangle = (1,0)^T$，$|e_2\rangle = (0,1)^T$，$|v\rangle = (3, 4i)^T$

1. 写出 $\langle v|$
2. 计算 $\langle v|v\rangle$（应为实数）
3. $\||v\rangle\|$ 是多少？
4. 用 $c_i = \langle e_i|v\rangle$ 展开 $|v\rangle$，验证结果

<details>
<summary>点击查看答案</summary>

1. $\langle v| = (3,\ -4i)$

2. $\langle v|v\rangle = 9 + (-4i)(4i) = 9 + 16 = 25$

3. $\||v\rangle\| = 5$

4. $c_1 = 3,\ c_2 = 4i$；$3(1,0)^T + 4i(0,1)^T = (3, 4i)^T = |v\rangle$ ✅

</details>

---

## 下一节

[04_第一章_线性算符.md](04_第一章_线性算符.md)

线性算符——量子力学中"可观测量"的数学语言，全章最重要的部分。
