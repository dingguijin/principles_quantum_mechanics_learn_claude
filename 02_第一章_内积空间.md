# 第一章第二节：内积空间（Inner Product Spaces）

> **对应 Shankar**：Chapter 1, Section 1.2  
> **前置知识**：[01_第一章_线性向量空间.md](01_第一章_线性向量空间.md)

---

## 从高中的点积说起

高中里，两个向量的**点积**（dot product）：

$$\vec{a} \cdot \vec{b} = a_x b_x + a_y b_y$$

几何意义：$\vec{a} \cdot \vec{b} = |\vec{a}|\ |\vec{b}|\ \cos\theta$

两个用途：
- **长度**：$|\vec{a}| = \sqrt{\vec{a} \cdot \vec{a}}$
- **垂直**：$\vec{a} \perp \vec{b} \iff \vec{a} \cdot \vec{b} = 0$

**内积**是点积推广到复数向量空间的版本。

---

## 内积的三条规则

两个向量 $|a\rangle$，$|b\rangle$ 的内积 $\langle a|b\rangle$ 是一个复数，满足：

| 规则 | 公式 | 说明 |
|------|------|------|
| 1. 交换取共轭 | $\langle a\|b\rangle = \langle b\|a\rangle^*$ | 交换顺序需取复共轭 |
| 2. 第二参数线性 | $\langle a\|\alpha b + \beta c\rangle = \alpha\langle a\|b\rangle + \beta\langle a\|c\rangle$ | 标量从第二个参数直接提出 |
| 3. 自内积非负 | $\langle a\|a\rangle \geq 0$，等号当且仅当 $\|a\rangle = \|0\rangle$ | 保证"长度"有意义 |

**由规则1+2推出**：第一个参数提取标量时要取复共轭（反线性）：

$$\langle \alpha a + \beta b | c \rangle = \alpha^* \langle a|c\rangle + \beta^* \langle b|c\rangle$$

---

## 具体例子

### $\mathbb{C}^n$ 中的内积

$$\langle a | b \rangle = \sum_{i=1}^n a_i^* b_i$$

注意：第一个向量的分量要取**复共轭**。

**数值例子**：$|a\rangle = (1+i,\ 2)$，$|b\rangle = (1,\ i)$

$$\langle a|b\rangle = (1+i)^* \cdot 1 + 2^* \cdot i = (1-i) + 2i = 1 + i$$

### 函数空间中的内积（量子力学核心）

$$\langle \psi | \phi \rangle = \int_{-\infty}^{+\infty} \psi^*(x)\ \phi(x)\ dx$$

第一个函数取复共轭，然后做积分。

---

## 范数（长度）

$$\| |a\rangle \| = \sqrt{\langle a | a \rangle}$$

**归一化**：任意非零向量可以变成单位向量：

$$|\hat{a}\rangle = \frac{|a\rangle}{\| |a\rangle \|}$$

---

## 正交（Orthogonality）

$$\langle a | b \rangle = 0 \iff |a\rangle \perp |b\rangle$$

---

## 正交归一基（Orthonormal Basis）⭐⭐⭐

基 $\{|e_1\rangle, \ldots, |e_n\rangle\}$ 是正交归一基，当且仅当：

$$\langle e_i | e_j \rangle = \delta_{ij} = \begin{cases} 1 & i = j \\ 0 & i \neq j \end{cases}$$

**核心用途**：若 $|v\rangle = \sum_i c_i |e_i\rangle$，则展开系数可以直接"读出"：

$$c_i = \langle e_i | v \rangle$$

推导：$\langle e_i | v \rangle = \sum_j c_j \langle e_i | e_j \rangle = \sum_j c_j \delta_{ij} = c_i$

---

## Bra $\langle a|$ 与 Ket $|a\rangle$

Dirac 把内积拆成两半：

$$\langle a | b \rangle = \underbrace{\langle a |}_{\text{bra（左矢）}} \underbrace{| b \rangle}_{\text{ket（右矢）}}$$

- $|b\rangle$（ket）：向量本身
- $\langle a|$（bra）：一个函数，吃进一个 ket，吐出一个复数

对应关系：

$$|a\rangle = \sum_i c_i |e_i\rangle \implies \langle a| = \sum_i c_i^* \langle e_i|$$

取 bra 时，系数要取**复共轭**。

---

## 本节核心

```
内积空间
  ├── 内积 ⟨a|b⟩ → 复数（三条规则）
  ├── 范数：‖|a⟩‖ = √⟨a|a⟩
  ├── 正交：⟨a|b⟩ = 0
  ├── 正交归一基：⟨eᵢ|eⱼ⟩ = δᵢⱼ，展开系数 cᵢ = ⟨eᵢ|v⟩
  └── Bra ⟨a|：ket |a⟩ 的对偶，系数取复共轭
```

---

## 小测试

设 $|a\rangle = (1, i)$，$|b\rangle = (i, 1)$（$\mathbb{C}^2$，内积 $\langle a|b\rangle = \sum_k a_k^* b_k$）

1. 计算 $\langle a|b\rangle$
2. 计算 $\langle b|a\rangle$，验证 $\langle a|b\rangle = \langle b|a\rangle^*$
3. 计算 $\||a\rangle\|$
4. $|a\rangle$ 和 $|b\rangle$ 正交吗？

<details>
<summary>点击查看答案</summary>

1. $\langle a|b\rangle = (1)^*(i) + (i)^*(1) = i + (-i) = 0$

2. $\langle b|a\rangle = (i)^*(1) + (1)^*(i) = -i + i = 0$；$\langle a|b\rangle^* = 0^* = 0$ ✅

3. $\langle a|a\rangle = 1 + (-i)(i) = 1 + 1 = 2$；$\||a\rangle\| = \sqrt{2}$

4. 是，$\langle a|b\rangle = 0$

</details>

---

## 下一节

[03_第一章_对偶空间与狄拉克符号.md](03_第一章_对偶空间与狄拉克符号.md)

深入理解 bra 是什么，完整建立 Dirac 符号体系。
