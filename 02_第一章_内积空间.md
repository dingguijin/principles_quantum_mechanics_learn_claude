# 第一章第二节：内积空间（Inner Product Spaces）

> **对应 Shankar**：Chapter 1, Section 1.2
> **前置知识**：[01_第一章_线性向量空间.md](01_第一章_线性向量空间.md)

---

## 一、向量空间缺少什么？

上一节我们定义了向量空间，有加法和数乘，满足 8 条公理。但这个结构还不够——它缺少两样东西：

- **长度**：向量有多"大"？
- **角度**：两个向量之间的夹角是多少？它们"垂直"吗？

对箭头向量，这些用**点积**（dot product）来回答：

$$\vec{a} \cdot \vec{b} = a_x b_x + a_y b_y$$

$$\vec{a} \cdot \vec{a} = a_x^2 + a_y^2 = \vert\vec{a}\vert^2 \quad \Rightarrow \quad \text{长度} = \sqrt{\vec{a}\cdot\vec{a}}$$

$$\vec{a} \perp \vec{b} \iff \vec{a}\cdot\vec{b} = 0 \quad \text{（垂直 $\Leftrightarrow$ 点积为零）}$$

现在，我们要把点积这个概念推广到任意向量空间（包括函数空间），并且允许复数。这个推广叫做**内积**（inner product）。

---

## 二、内积的定义

设 $V$ 是一个向量空间（标量是复数）。**内积**是一个操作，把两个向量 $\vert a\rangle$，$\vert b\rangle$ 映射到一个复数，记作：

$$\langle a \vert b \rangle$$

读作 "bracket a b" 或 "a 与 b 的内积"。

内积必须满足 3 条规则：

### 规则 1：交换顺序，结果取复共轭

$$\langle a \vert b \rangle = \langle b \vert a \rangle^*$$

注意：不是简单相等，而是**相等后取复共轭**。

对实数向量（系数都是实数），$\langle b\vert a\rangle^* = \langle b\vert a\rangle$，所以退化成 $\langle a\vert b\rangle = \langle b\vert a\rangle$，和普通点积一样（对称）。

对复数向量，交换顺序结果不相等，而是互为复共轭。

### 规则 2：对第二个参数是线性的

$$\langle a \vert \alpha\vert b\rangle + \beta\vert c\rangle \rangle = \alpha \langle a \vert b \rangle + \beta \langle a \vert c \rangle$$

也就是说，如果第二个向量是一个线性组合，可以把系数"提出来"。

### 规则 3：自内积非负，且等于零当且仅当零向量

$$\langle a \vert a \rangle \geq 0$$

$$\langle a \vert a \rangle = 0 \iff \vert a \rangle = \vert 0 \rangle$$

这保证了"长度"是有意义的非负实数。

---

## 三、从规则 1+2 推出第一个参数的反线性性

这是一个重要推论，要仔细看。

**问**：如果第一个参数是线性组合，系数怎么提出来？

$$\langle \alpha\vert a\rangle + \beta\vert b\rangle \vert c \rangle = \, ?$$

用规则 1 把顺序换一下：

$$\langle \alpha\vert a\rangle + \beta\vert b\rangle \vert c \rangle = \langle c \vert \alpha\vert a\rangle + \beta\vert b\rangle \rangle^*$$

用规则 2 对右边展开：

$$= \bigl(\alpha\langle c\vert a\rangle + \beta\langle c\vert b\rangle\bigr)^*$$

取复共轭，注意 $(uv)^* = u^* v^*$ 和 $(u+v)^* = u^*+v^*$：

$$= \alpha^*\langle c\vert a\rangle^* + \beta^*\langle c\vert b\rangle^*$$

再用规则 1 把每项换回来：

$$= \alpha^*\langle a\vert c\rangle + \beta^*\langle b\vert c\rangle$$

**结论**：

$$\boxed{\langle \alpha\vert a\rangle + \beta\vert b\rangle \vert c \rangle = \alpha^*\langle a\vert c\rangle + \beta^*\langle b\vert c\rangle}$$

对第一个参数提取系数时，**需要取复共轭**。

这个性质叫做**反线性**（anti-linear）或共轭线性（conjugate-linear）。

> 为什么规则 2 是对第二个参数线性，而不是对第一个？这是物理学家（Shankar 也是）的约定。数学家有时候反过来定义。两种约定都合法，但要一致。

---

## 四、为什么规则 1 不能是简单的对称？

这里有一个微妙但重要的原因，值得解释清楚。

**假设**我们像实数点积那样定义：$\langle a\vert b\rangle = \langle b\vert a\rangle$（简单对称）。

结合规则 2（第二参数线性）：对第一个参数，若 $\langle \alpha a\vert b\rangle = \langle b\vert\alpha a\rangle = \alpha\langle b\vert a\rangle = \alpha\langle a\vert b\rangle$。

现在看自内积：$\langle \alpha a\vert \alpha a\rangle = \alpha\langle a\vert\alpha a\rangle = \alpha^2\langle a\vert a\rangle$。

如果 $\alpha = i$（虚数单位），则 $\langle ia\vert ia\rangle = i^2\langle a\vert a\rangle = -\langle a\vert a\rangle$。

但规则 3 要求 $\langle ia\vert ia\rangle \geq 0$，而 $-\langle a\vert a\rangle \leq 0$——矛盾！

**所以**：对于复数向量空间，内积不能简单对称。必须是规则 1 这样的"共轭对称"。而这自然导致第一个参数是反线性的。

这一切都是为了保证 $\langle a\vert a\rangle$ 是一个**非负实数**。

---

## 五、具体内积公式：$\mathbb{C}^n$ 中的例子

设 $V = \mathbb{C}^n$（$n$ 维复数列向量），向量写成列：

$$\vert a\rangle = \begin{pmatrix}a_1\\a_2\\\vdots\\a_n\end{pmatrix}, \quad \vert b\rangle = \begin{pmatrix}b_1\\b_2\\\vdots\\b_n\end{pmatrix}$$

内积定义为：

$$\langle a\vert b\rangle = \sum_{k=1}^n a_k^* b_k = a_1^* b_1 + a_2^* b_2 + \cdots + a_n^* b_n$$

注意：第一个向量的分量要取**复共轭**（$a_k^*$），第二个不取（$b_k$）。

### 验证规则 1

$$\langle b\vert a\rangle = \sum_k b_k^* a_k$$

$$\langle a\vert b\rangle^* = \left(\sum_k a_k^* b_k\right)^* = \sum_k a_k b_k^* = \sum_k b_k^* a_k = \langle b\vert a\rangle \checkmark$$

### 具体计算例子

设 $\vert a\rangle = \begin{pmatrix}1+i\\2\end{pmatrix}$，$\vert b\rangle = \begin{pmatrix}1\\i\end{pmatrix}$

$$\langle a\vert b\rangle = (1+i)^* \cdot 1 + 2^* \cdot i = (1-i)\cdot 1 + 2\cdot i = 1-i+2i = 1+i$$

$$\langle b\vert a\rangle = 1^*\cdot(1+i) + i^*\cdot 2 = 1\cdot(1+i) + (-i)\cdot 2 = 1+i-2i = 1-i$$

验证：$\langle a\vert b\rangle^* = (1+i)^* = 1-i = \langle b\vert a\rangle$ ✓

---

## 六、具体内积公式：函数空间（量子力学的核心！）

设 $V$ 是所有定义在实数轴上的复值函数，内积定义为：

$$\langle \psi\vert\phi\rangle = \int_{-\infty}^{+\infty} \psi^*(x)\,\phi(x)\,dx$$

规则：第一个函数取复共轭，然后乘以第二个函数，再在整个实数轴上积分。

这个积分公式你目前可能还不熟悉积分，但需要记住它的形式——在量子力学里，这是计算概率的核心公式。

量子力学中，$\vert\langle a\vert b\rangle\vert^2$ 给出的是系统从状态 $\vert b\rangle$ 被测量到状态 $\vert a\rangle$ 的概率。

---

## 七、范数（Norm）：向量的长度

利用内积，我们可以定义向量的**范数**（norm，也叫长度）：

$$\Vert\, \vert a\rangle\,\Vert = \sqrt{\langle a\vert a\rangle}$$

由规则 3，$\langle a\vert a\rangle \geq 0$，所以开方有意义，且范数是非负实数。

**例子**（$\mathbb{C}^2$）：$\vert a\rangle = \begin{pmatrix}3\\4\end{pmatrix}$（实数分量）

$$\langle a\vert a\rangle = 3^*\cdot 3 + 4^*\cdot 4 = 9 + 16 = 25 \implies \Vert\,\vert a\rangle\,\Vert = 5$$

和高中公式 $\sqrt{3^2+4^2} = 5$ 完全一致 ✓

**例子**（复数分量）：$\vert v\rangle = \begin{pmatrix}1+i\\2i\end{pmatrix}$

$$\langle v\vert v\rangle = (1+i)^*(1+i) + (2i)^*(2i) = \vert 1+i\vert^2 + \vert 2i\vert^2 = 2 + 4 = 6$$

$$\Vert\,\vert v\rangle\,\Vert = \sqrt{6}$$

### 单位向量与归一化

**单位向量**（unit vector）：范数为 1 的向量，$\Vert\,\vert a\rangle\,\Vert = 1$。

**归一化**（normalization）：把任意非零向量变成单位向量：

$$\vert\hat{a}\rangle = \frac{\vert a\rangle}{\Vert\,\vert a\rangle\,\Vert}$$

验证：$\langle\hat{a}\vert\hat{a}\rangle = \dfrac{\langle a\vert a\rangle}{\Vert\vert a\rangle\Vert^2} = \dfrac{\langle a\vert a\rangle}{\langle a\vert a\rangle} = 1$ ✓

**例子**：归一化 $\vert a\rangle = \begin{pmatrix}3\\4\end{pmatrix}$：

$$\vert\hat{a}\rangle = \frac{1}{5}\begin{pmatrix}3\\4\end{pmatrix} = \begin{pmatrix}3/5\\4/5\end{pmatrix}$$

---

## 八、正交（Orthogonality）：推广的"垂直"

两个向量 $\vert a\rangle$ 和 $\vert b\rangle$ **正交**（orthogonal），如果：

$$\langle a\vert b\rangle = 0$$

这是"垂直"（点积为零）的推广。

**例子**：$\vert e_1\rangle = (1,0)^T$ 和 $\vert e_2\rangle = (0,1)^T$

$$\langle e_1\vert e_2\rangle = 1^*\cdot 0 + 0^*\cdot 1 = 0 \checkmark \quad \text{正交}$$

**复数例子**：$\vert a\rangle = (1,i)^T$ 和 $\vert b\rangle = (i,1)^T$

$$\langle a\vert b\rangle = 1^*\cdot i + i^*\cdot 1 = i + (-i) = 0 \checkmark \quad \text{正交}$$

---

## 九、正交归一基（Orthonormal Basis）⭐⭐⭐

一组基 $\{\vert e_1\rangle, \vert e_2\rangle, \ldots, \vert e_n\rangle\}$ 叫做**正交归一基**（orthonormal basis），如果：

$$\langle e_i \vert e_j \rangle = \delta_{ij}$$

其中 $\delta_{ij}$ 是 **Kronecker delta**：

$$\delta_{ij} = \begin{cases}1 & \text{若 } i = j \\ 0 & \text{若 } i \neq j\end{cases}$$

用文字说：**每个基向量是单位向量（$\langle e_i\vert e_i\rangle = 1$），且任意两个不同基向量互相正交（$\langle e_i\vert e_j\rangle = 0$）。**

### 为什么正交归一基如此重要？

因为它让展开系数的计算变得极其简单。

设 $\vert v\rangle = \sum_j c_j \vert e_j\rangle$，怎么求系数 $c_i$？

用 $\langle e_i\vert$ 从左边作用上去：

$$\langle e_i\vert v\rangle = \left\langle e_i\,\Big\vert\, \sum_j c_j\vert e_j\rangle\right\rangle = \sum_j c_j \langle e_i\vert e_j\rangle = \sum_j c_j \delta_{ij} = c_i$$

因此：

$$\boxed{c_i = \langle e_i\vert v\rangle}$$

**展开系数 = 基向量与被展开向量的内积**。这就像用内积"筛出"每个方向的分量。

### 具体例子

$\mathbb{R}^2$ 的标准基 $\vert e_1\rangle = (1,0)^T$，$\vert e_2\rangle = (0,1)^T$，展开 $\vert v\rangle = (3,5)^T$：

$$c_1 = \langle e_1\vert v\rangle = 1\cdot 3 + 0\cdot 5 = 3$$
$$c_2 = \langle e_2\vert v\rangle = 0\cdot 3 + 1\cdot 5 = 5$$

所以 $\vert v\rangle = 3\vert e_1\rangle + 5\vert e_2\rangle = 3(1,0)^T + 5(0,1)^T = (3,5)^T$ ✓

---

## 十、Bra $\langle a\vert$ 初识

现在我们可以理解 Dirac 的符号设计了。

内积 $\langle a\vert b\rangle$ 被看成两部分：

$$\underbrace{\langle a\vert}_{\text{bra}} \cdot \underbrace{\vert b\rangle}_{\text{ket}}$$

- $\vert b\rangle$（ket，右矢）：向量本身
- $\langle a\vert$（bra，左矢）：一个"接收器"，吃进一个 ket，吐出复数 $\langle a\vert b\rangle$

合起来叫 **bra-ket**，即 bracket（括号）——这是名字的由来。

在列向量表示中：

$$\vert a\rangle = \begin{pmatrix}a_1\\a_2\end{pmatrix} \quad \longleftrightarrow \quad \langle a\vert = \begin{pmatrix}a_1^*&a_2^*\end{pmatrix}$$

ket 是列向量，bra 是取了复共轭的行向量（即**厄米共轭**，记作 $\dagger$）。

内积就是行向量乘列向量：

$$\langle a\vert b\rangle = \begin{pmatrix}a_1^*&a_2^*\end{pmatrix}\begin{pmatrix}b_1\\b_2\end{pmatrix} = a_1^*b_1 + a_2^*b_2$$

---

## 十一、本节总结

```
内积空间
│
├── 内积 ⟨a|b⟩ → 复数，三条规则：
│     1. ⟨a|b⟩ = ⟨b|a⟩*（共轭对称）
│     2. 对第二参数线性
│     3. ⟨a|a⟩ ≥ 0（自内积非负）
│
├── 推论：对第一参数反线性（系数取共轭）
│
├── 为何要共轭对称？
│     → 保证 ⟨a|a⟩ 是非负实数（复数不能有序比较大小）
│
├── C^n 的内积：⟨a|b⟩ = Σ aₖ* bₖ
│
├── 函数空间的内积：∫ ψ*(x) φ(x) dx（量子力学核心）
│
├── 范数（长度）：‖|a⟩‖ = √⟨a|a⟩
│     └── 归一化：|â⟩ = |a⟩ / ‖|a⟩‖
│
├── 正交：⟨a|b⟩ = 0
│
├── 正交归一基：⟨eᵢ|eⱼ⟩ = δᵢⱼ
│     └── 展开系数：cᵢ = ⟨eᵢ|v⟩
│
└── Bra ⟨a|：ket |a⟩ 对应的行向量（分量取复共轭）
```

---

## 小测试

设 $\vert a\rangle = \begin{pmatrix}1\\i\end{pmatrix}$，$\vert b\rangle = \begin{pmatrix}2\\1-i\end{pmatrix}$（$\mathbb{C}^2$）

1. 计算 $\langle a\vert b\rangle$
2. 计算 $\langle b\vert a\rangle$，验证 $\langle a\vert b\rangle = \langle b\vert a\rangle^*$
3. 计算 $\Vert\,\vert a\rangle\,\Vert$ 并将 $\vert a\rangle$ 归一化
4. 写出 $\langle a\vert$ 的行向量形式

<details>
<summary>点击查看答案</summary>

1. $\langle a\vert b\rangle = 1^* \cdot 2 + i^*\cdot(1-i) = 1\cdot 2 + (-i)(1-i) = 2 + (-i+i^2) = 2 + (-i-1) = 1-i$

2. $\langle b\vert a\rangle = 2^*\cdot 1 + (1-i)^*\cdot i = 2 + (1+i)\cdot i = 2 + i + i^2 = 2 + i - 1 = 1+i$

   验证：$\langle a\vert b\rangle^* = (1-i)^* = 1+i = \langle b\vert a\rangle$ ✓

3. $\langle a\vert a\rangle = 1^*\cdot 1 + i^*\cdot i = 1 + (-i)(i) = 1 + 1 = 2$，所以 $\Vert\,\vert a\rangle\,\Vert = \sqrt{2}$

   归一化：$\vert\hat{a}\rangle = \dfrac{1}{\sqrt{2}}\begin{pmatrix}1\\i\end{pmatrix}$

4. $\langle a\vert = \begin{pmatrix}1^* & i^*\end{pmatrix} = \begin{pmatrix}1 & -i\end{pmatrix}$

</details>

---

下一节：[03_第一章_对偶空间与狄拉克符号.md](03_第一章_对偶空间与狄拉克符号.md)

深入理解 bra 的本质，以及完整的 Dirac 符号体系。
