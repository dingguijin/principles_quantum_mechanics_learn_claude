# 第一章第二节：内积空间（Inner Product Spaces）

> 对应 Shankar Chapter 1, Section 1.2
> 前置知识：[01_第一章_线性向量空间.md](01_第一章_线性向量空间.md)

---

## 向量空间还缺什么？

向量空间有加法和数乘，但没有"长度"和"角度"的概念。内积就是为了补上这两样。

---

## 出发点：高中的点积

实数向量 $\vec{a}=(a_1,a_2)$，$\vec{b}=(b_1,b_2)$ 的点积：

$$\vec{a}\cdot\vec{b} = a_1 b_1 + a_2 b_2$$

用途：**长度** $\vert\vec{a}\vert = \sqrt{\vec{a}\cdot\vec{a}}$，**垂直** $\vec{a}\perp\vec{b}\iff\vec{a}\cdot\vec{b}=0$。

---

## 复数带来的麻烦

若向量有复数分量，直接套点积公式会出问题。

例：$\vert v\rangle = (i,0)^T$，用实数公式：$\vert v\rangle\cdot\vert v\rangle = i\cdot i = i^2 = -1 < 0$。

但长度的平方不能是负数！

**修复**：第一个向量的分量取复共轭：

$$\langle v\vert v\rangle = (i)^*\cdot i = (-i)(i) = -i^2 = 1 \geq 0 \checkmark$$

---

## 内积的 3 条规则

内积 $\langle a\vert b\rangle$ 输入两个向量，输出一个复数，满足：

**规则 1**（共轭对称）：$\langle a\vert b\rangle = \langle b\vert a\rangle^*$

**规则 2**（第二参数线性）：$\langle a\vert \alpha\vert b\rangle+\beta\vert c\rangle\rangle = \alpha\langle a\vert b\rangle+\beta\langle a\vert c\rangle$

**规则 3**（自内积非负）：$\langle a\vert a\rangle\geq 0$，等号 $\iff\vert a\rangle=\vert 0\rangle$

---

## 重要推论：第一参数提标量要取共轭（反线性）

$$\langle\alpha a\vert b\rangle = \langle b\vert\alpha a\rangle^* = (\alpha\langle b\vert a\rangle)^* = \alpha^*\langle b\vert a\rangle^* = \alpha^*\langle a\vert b\rangle$$

**对第一个向量，标量要取复共轭才能提出来。**

---

## $\mathbb{C}^n$ 中的内积

$$\langle a\vert b\rangle = \sum_{k=1}^n a_k^* b_k$$

第一个向量分量取复共轭，第二个不取。

**例**：$\vert a\rangle=(1+i,2)^T$，$\vert b\rangle=(1,i)^T$

$$\langle a\vert b\rangle = (1-i)\cdot 1 + 2\cdot i = 1-i+2i = 1+i$$

---

## 函数空间中的内积（量子力学核心）

$$\langle\psi\vert\phi\rangle = \int_{-\infty}^{+\infty}\psi^*(x)\,\phi(x)\,dx$$

第一个函数取复共轭，逐点相乘后对全实数轴积分。（积分是求和的连续推广。）

---

## 范数（长度）

$$\Vert\vert a\rangle\Vert = \sqrt{\langle a\vert a\rangle}$$

**归一化**：$\vert\hat{a}\rangle = \vert a\rangle\,/\,\Vert\vert a\rangle\Vert$，使范数等于 1。

**例**：$\vert a\rangle=(3,4)^T$，$\langle a\vert a\rangle=25$，$\Vert\vert a\rangle\Vert=5$。

---

## 正交

$$\langle a\vert b\rangle = 0 \iff \vert a\rangle \perp \vert b\rangle$$

---

## 正交归一基（Orthonormal Basis）⭐

基 $\{\vert e_i\rangle\}$ 满足 $\langle e_i\vert e_j\rangle = \delta_{ij}$（$i=j$ 时为 1，否则为 0）。

**核心公式**：在正交归一基下，$\vert v\rangle=\sum_i c_i\vert e_i\rangle$ 的展开系数：

$$\boxed{c_i = \langle e_i\vert v\rangle}$$

推导：$\langle e_i\vert v\rangle = \sum_j c_j\langle e_i\vert e_j\rangle = \sum_j c_j\delta_{ij} = c_i$

量子力学意义：$\vert c_i\vert^2$ 是测量到第 $i$ 个结果的概率。

---

## 练习

$\vert a\rangle=(1,2i)^T$，$\vert b\rangle=(i,1)^T$，$\vert e_1\rangle=(1,0)^T$，$\vert e_2\rangle=(0,1)^T$，$\vert v\rangle=(3+i,2)^T$

1. 计算 $\langle a\vert b\rangle$ 和 $\langle b\vert a\rangle$，验证互为复共轭
2. 计算 $\Vert\vert a\rangle\Vert$，写出 $\vert\hat{a}\rangle$
3. 用 $c_i=\langle e_i\vert v\rangle$ 展开 $\vert v\rangle$，验证结果

<details>
<summary>答案</summary>

1. $\langle a\vert b\rangle = i+(-2i) = -i$；$\langle b\vert a\rangle = -i+2i = i$；$(-i)^* = i$ ✓

2. $\langle a\vert a\rangle = 1+4=5$；$\Vert\vert a\rangle\Vert=\sqrt{5}$；$\vert\hat{a}\rangle = \frac{1}{\sqrt{5}}(1,2i)^T$

3. $c_1=3+i$，$c_2=2$；$(3+i)(1,0)^T+2(0,1)^T=(3+i,2)^T$ ✓

</details>

---

下一节：[03_第一章_对偶空间与狄拉克符号.md](03_第一章_对偶空间与狄拉克符号.md)
