# 第一章第八节：本征值问题（The Eigenvalue Problem）

> 对应 Shankar Chapter 1, Section 1.8
> 前置知识：[04_第一章_线性算符.md](04_第一章_线性算符.md)

---

## 特殊的向量：作用后方向不变

大多数向量经过算符作用后，方向会改变。但有些特殊向量，作用后只是被"拉伸"或"压缩"，方向不变。

**例**：$\hat{A}=\begin{pmatrix}2&1\\1&2\end{pmatrix}$ 作用在 $(1,1)^T$ 上得 $(3,3)^T=3\cdot(1,1)^T$。方向不变，缩放 3 倍。

---

## 定义

若非零向量 $\vert v\rangle$ 满足：

$$\hat{A}\vert v\rangle = \lambda\vert v\rangle$$

则 $\vert v\rangle$ 是 $\hat{A}$ 的**本征向量**（eigenvector），$\lambda$ 是对应的**本征值**（eigenvalue）。

---

## 找本征值：特征方程

本征方程 $\Leftrightarrow$ $(\hat{A}-\lambda\hat{I})\vert v\rangle=\vert 0\rangle$ 有非零解 $\Leftrightarrow$：

$$\det(\hat{A}-\lambda\hat{I}) = 0$$

**行列式**（$2\times2$）：$\det\begin{pmatrix}a&b\\c&d\end{pmatrix}=ad-bc$

---

## 完整求解例子

$\hat{A}=\begin{pmatrix}2&1\\1&2\end{pmatrix}$

**第一步**——特征方程：

$$\det\begin{pmatrix}2-\lambda&1\\1&2-\lambda\end{pmatrix}=(2-\lambda)^2-1=\lambda^2-4\lambda+3=(\lambda-1)(\lambda-3)=0$$

本征值：$\lambda_1=1$，$\lambda_2=3$

**第二步**——对 $\lambda_1=1$，解 $(\hat{A}-I)\vert v\rangle=0$：

$$\begin{pmatrix}1&1\\1&1\end{pmatrix}\begin{pmatrix}x\\y\end{pmatrix}=0\implies y=-x$$

本征向量：$\vert v_1\rangle=\frac{1}{\sqrt{2}}(1,-1)^T$

**对 $\lambda_2=3$**：$y=x$，本征向量：$\vert v_2\rangle=\frac{1}{\sqrt{2}}(1,1)^T$

---

## 厄米算符的两个关键性质（附证明）

### 性质一：本征值是实数

设 $\hat{A}^\dagger=\hat{A}$，$\hat{A}\vert v\rangle=\lambda\vert v\rangle$，$\vert v\rangle\neq\vert 0\rangle$。

用 $\langle v\vert$ 从左作用：$\langle v\vert\hat{A}\vert v\rangle=\lambda\langle v\vert v\rangle$

另一方面，由厄米共轭定义：$\langle v\vert\hat{A}\vert v\rangle^*=\langle v\vert\hat{A}^\dagger\vert v\rangle=\langle v\vert\hat{A}\vert v\rangle$

故 $\lambda\langle v\vert v\rangle=\lambda^*\langle v\vert v\rangle$，因 $\langle v\vert v\rangle>0$，得 $\lambda=\lambda^*$，即 $\lambda$ 是实数。$\square$

### 性质二：不同本征值的本征向量正交

设 $\hat{A}\vert a\rangle=a\vert a\rangle$，$\hat{A}\vert b\rangle=b\vert b\rangle$，$a\neq b$。

计算 $\langle b\vert\hat{A}\vert a\rangle$：

- 方式一：$=a\langle b\vert a\rangle$
- 方式二：利用 $\langle b\vert\hat{A}=b\langle b\vert$（因 $b$ 是实数且 $\hat{A}^\dagger=\hat{A}$）：$=b\langle b\vert a\rangle$

所以 $(a-b)\langle b\vert a\rangle=0$，因 $a\neq b$，得 $\langle b\vert a\rangle=0$。$\square$

---

## 谱分解（Spectral Decomposition）

厄米算符 $\hat{A}$ 可以展开为：

$$\boxed{\hat{A}=\sum_i a_i\,\vert a_i\rangle\langle a_i\vert}$$

其中 $\{a_i\}$ 是本征值，$\{\vert a_i\rangle\}$ 是构成正交归一基的本征向量。

验证（上例）：$1\cdot\vert v_1\rangle\langle v_1\vert+3\cdot\vert v_2\rangle\langle v_2\vert=\begin{pmatrix}2&1\\1&2\end{pmatrix}=\hat{A}$ ✓

---

## 量子力学意义

对可观测量 $\hat{A}$（厄米算符），量子态 $\vert\psi\rangle=\sum_i c_i\vert a_i\rangle$：

| 量 | 含义 |
|----|------|
| 本征值 $a_i$ | 测量可能得到的结果（实数） |
| 本征向量 $\vert a_i\rangle$ | 测量后系统坍缩到的状态 |
| $c_i=\langle a_i\vert\psi\rangle$ | 展开系数 |
| $\vert c_i\vert^2$ | 得到结果 $a_i$ 的概率 |

---

## 练习

$\hat{B}=\begin{pmatrix}0&1\\1&0\end{pmatrix}$（Pauli $X$ 矩阵）

1. 验证 $\hat{B}$ 是厄米算符
2. 求本征值（解 $\det(\hat{B}-\lambda I)=0$）
3. 求每个本征值对应的归一化本征向量
4. 验证两本征向量正交
5. 写出谱分解并验证

<details>
<summary>答案</summary>

1. $\hat{B}^T=\hat{B}$，实对称，故厄米 ✓

2. $\lambda^2-1=0$，$\lambda=\pm1$

3. $\lambda=1$：$\vert+\rangle=\frac{1}{\sqrt{2}}(1,1)^T$；$\lambda=-1$：$\vert-\rangle=\frac{1}{\sqrt{2}}(1,-1)^T$

4. $\langle+\vert-\rangle=\frac{1}{2}(1-1)=0$ ✓

5. $1\cdot\vert+\rangle\langle+\vert+(-1)\vert-\rangle\langle-\vert=\frac{1}{2}\begin{pmatrix}1&1\\1&1\end{pmatrix}-\frac{1}{2}\begin{pmatrix}1&-1\\-1&1\end{pmatrix}=\begin{pmatrix}0&1\\1&0\end{pmatrix}=\hat{B}$ ✓

</details>

---

下一節：[06_第一章_无限维推广.md](06_第一章_无限维推广.md)
