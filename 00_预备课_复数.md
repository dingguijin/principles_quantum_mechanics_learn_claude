# 预备课：复数（Complex Numbers）

> **对应 Shankar 章节**：第一章正式开始前的数学准备  
> **前置知识**：无（从零开始）

---

## 为什么需要复数？

从实数说起。你知道：

$$x^2 = 4 \implies x = \pm 2$$

但如果有人问：$x^2 = -1$，$x$ 等于多少？

在实数范围内，**没有答案**——任何实数的平方都是非负数。

为了解决这个问题，数学家引入了一个"假想"的数：

$$\boxed{i = \sqrt{-1}}$$

这个 $i$ 叫做**虚数单位**（imaginary unit）。$i$ 是人为定义的，但它极其有用，量子力学到处都是它。

---

## 复数的定义

一个**复数**（complex number）写成这种形式：

$$z = a + bi$$

其中：
- $a$ 是**实部**（real part），记作 $\operatorname{Re}(z) = a$
- $b$ 是**虚部**（imaginary part），记作 $\operatorname{Im}(z) = b$
- $a, b$ 都是普通实数

**例子：**

| 复数 $z$ | 实部 $a$ | 虚部 $b$ | 备注 |
|----------|----------|----------|------|
| $3 + 4i$ | $3$ | $4$ | 普通复数 |
| $-1 + 2i$ | $-1$ | $2$ | |
| $5$ | $5$ | $0$ | 普通实数是复数的特例 |
| $3i$ | $0$ | $3$ | 纯虚数 |

---

## 复数的四则运算

设 $z_1 = a + bi$，$z_2 = c + di$

**加法**——实部加实部，虚部加虚部：

$$z_1 + z_2 = (a+c) + (b+d)i$$

**乘法**——像多项式一样展开，再用 $i^2 = -1$：

$$z_1 \times z_2 = (ac - bd) + (ad + bc)i$$

推导过程：

$$
(a+bi)(c+di) = ac + adi + bci + bdi^2 = ac + adi + bci - bd
$$

**例子**：$(1+2i)(3+4i)$

$$
= 3 + 4i + 6i + 8i^2 = 3 + 10i - 8 = -5 + 10i
$$

---

## 复共轭（Complex Conjugate）⭐ 极其重要

给定 $z = a + bi$，它的**复共轭**定义为：

$$z^* = a - bi \quad \text{（把虚部符号取反）}$$

> 有些书写作 $\bar{z}$，意思相同。

**关键性质**：

$$z \cdot z^* = (a+bi)(a-bi) = a^2 + b^2 \quad \text{（结果是非负实数！）}$$

这个性质在量子力学中会反复使用。

---

## 复数的模（Modulus）

$z = a + bi$ 的**模**（modulus）：

$$|z| = \sqrt{a^2 + b^2} = \sqrt{z \cdot z^*}$$

可以理解为复数的"大小"或"长度"。

**例子**：$|3+4i| = \sqrt{9+16} = \sqrt{25} = 5$

---

## 复平面（Complex Plane）

把复数 $z = a + bi$ 看成平面上的一个点：

- 横轴（实轴）代表实部 $a$
- 纵轴（虚轴）代表虚部 $b$

```
虚轴 (Im)
  ↑
  |       × z = a + bi
  |      /|
  |   r / |
  |    /  |
  |   / θ |
  |  /    |
--+--------→ 实轴 (Re)
  O    a
```

在这个图里：
- $r = |z| = \sqrt{a^2+b^2}$ 是点到原点的**距离**
- $\theta = \arctan(b/a)$ 是与实轴的**夹角**，叫**辐角**（argument），记作 $\arg(z)$

---

## 极坐标形式与欧拉公式 ⭐⭐ 量子力学最常用

用模 $r$ 和辐角 $\theta$，复数可以写成**极坐标形式**：

$$z = r(\cos\theta + i\sin\theta)$$

更简洁的写法来自**欧拉公式**（Euler's formula）：

$$\boxed{e^{i\theta} = \cos\theta + i\sin\theta}$$

所以：

$$z = r e^{i\theta}$$

**特殊值**：

| $\theta$ | $e^{i\theta}$ | 等于 |
|----------|--------------|------|
| $0$ | $e^{0} = 1$ | $\cos 0 + i\sin 0 = 1$ |
| $\pi/2$ | $e^{i\pi/2}$ | $\cos(\pi/2) + i\sin(\pi/2) = i$ |
| $\pi$ | $e^{i\pi}$ | $\cos\pi + i\sin\pi = -1$ |
| $2\pi$ | $e^{2\pi i}$ | $\cos 2\pi + i\sin 2\pi = 1$ |

由 $e^{i\pi} = -1$ 得到著名的**欧拉恒等式**：

$$e^{i\pi} + 1 = 0$$

---

## 小测试

自己做完再看答案：

1. $(2+3i) + (1-i) = ?$
2. $(1+i)^2 = ?$
3. $|1+i| = ?$
4. $(1+i)$ 的复共轭是什么？
5. $e^{i \cdot 0} = ?$（用欧拉公式验证）

<details>
<summary>点击查看答案</summary>

1. $(2+1) + (3-1)i = 3 + 2i$
2. $1 + 2i + i^2 = 1 + 2i - 1 = 2i$
3. $\sqrt{1^2 + 1^2} = \sqrt{2}$
4. $1 - i$
5. $\cos 0 + i\sin 0 = 1 + 0 = 1$

</details>

---

## 下一课预告

[01_第一章_线性向量空间.md](01_第一章_线性向量空间.md)

从你熟悉的平面向量出发，逐步建立**抽象向量空间**的概念——这是 Shankar 第一章的核心，也是整个量子力学的数学骨架。
