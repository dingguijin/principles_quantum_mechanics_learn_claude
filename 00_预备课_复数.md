# 预备课：复数（Complex Numbers）

> **对应 Shankar**：第一章正式开始前的数学准备
> **前置知识**：无（从零开始）

---

## 一、为什么需要复数？

我们先从你熟悉的数开始讲起。

### 数的扩展历史

人类对"数"的认识是一步步扩展的：

- 最开始只有**正整数**：1, 2, 3, ...（数苹果用的）
- 后来引入**零和负数**：..., -2, -1, 0, 1, 2, ...（表示债务、温度等）
- 再后来引入**分数**：1/2, 3/4, ...（切分东西用的）
- 然后是**无理数**：$\sqrt{2}$, $\pi$, ...（几何中的长度）

这些合在一起叫**实数**（real numbers）。

现在问你：方程 $x^2 = 4$ 的解是多少？

$$x = 2 \quad \text{或} \quad x = -2$$

很好。那 $x^2 = -1$ 呢？

在实数范围里，**没有解**。因为任何实数的平方都是非负数：正数的平方是正数，负数的平方也是正数，零的平方是零。所以平方等于 $-1$ 的实数根本不存在。

数学家们面临一个选择：要么说"这个方程无解"，要么**扩展数的范围**。他们选择了后者，引入了一个新的数：

$$\boxed{i = \sqrt{-1}, \quad \text{即} \quad i^2 = -1}$$

这个 $i$ 叫做**虚数单位**（imaginary unit）。"虚"字不是说它不重要或者假的，只是历史上的叫法。事实上，$i$ 在物理、工程、量子力学中无处不在，极其"实用"。

---

## 二、复数的定义

有了 $i$，我们可以定义**复数**（complex number）：

$$z = a + bi$$

其中：
- $a$ 是**实部**（real part），写作 $\operatorname{Re}(z) = a$
- $b$ 是**虚部**（imaginary part），写作 $\operatorname{Im}(z) = b$
- $a$ 和 $b$ 都是普通的实数

**注意**：虚部 $b$ 本身是实数，是乘以 $i$ 的那个系数，而不是 $bi$ 整体。

### 例子

| 复数 $z$ | 实部 $\operatorname{Re}(z)$ | 虚部 $\operatorname{Im}(z)$ | 类型 |
|----------|----------|----------|------|
| $3 + 4i$ | $3$ | $4$ | 普通复数 |
| $-1 + 2i$ | $-1$ | $2$ | 普通复数 |
| $5 + 0i = 5$ | $5$ | $0$ | 实数（复数的特例） |
| $0 + 3i = 3i$ | $0$ | $3$ | 纯虚数 |
| $0 + 0i = 0$ | $0$ | $0$ | 零 |

**关键认识**：实数是复数的特殊情况（虚部为 0）。复数是实数的扩展，不是替换。

---

## 三、复数的四则运算

设 $z_1 = a + bi$，$z_2 = c + di$

### 加法和减法

实部与实部相加，虚部与虚部相加：

$$z_1 + z_2 = (a + c) + (b + d)i$$

$$z_1 - z_2 = (a - c) + (b - d)i$$

**例子**：$(3 + 2i) + (1 - 5i) = (3+1) + (2-5)i = 4 - 3i$

**例子**：$(3 + 2i) - (1 - 5i) = (3-1) + (2-(-5))i = 2 + 7i$

### 乘法

像普通的多项式乘法一样展开，然后用 $i^2 = -1$ 化简：

$$(a + bi)(c + di) = ac + adi + bci + bdi^2$$

因为 $i^2 = -1$：

$$= ac + adi + bci - bd = (ac - bd) + (ad + bc)i$$

**例子**：$(1 + 2i)(3 + 4i)$

一步一步来：

$$= 1 \cdot 3 + 1 \cdot 4i + 2i \cdot 3 + 2i \cdot 4i$$

$$= 3 + 4i + 6i + 8i^2$$

$$= 3 + 10i + 8 \cdot (-1)$$

$$= 3 + 10i - 8 = -5 + 10i$$

**例子**：$i^2 = i \cdot i = -1$（定义）

**例子**：$i^3 = i^2 \cdot i = -1 \cdot i = -i$

**例子**：$i^4 = i^2 \cdot i^2 = (-1)(-1) = 1$

> $i$ 的幂次是循环的：$i^1 = i$，$i^2 = -1$，$i^3 = -i$，$i^4 = 1$，然后又从 $i^5 = i$ 开始循环。

---

## 四、复共轭（Complex Conjugate）⭐ 最重要的操作

给定 $z = a + bi$，它的**复共轭**（complex conjugate）是：

$$z^* = a - bi$$

操作很简单：**把虚部的符号取反**，实部不变。

有些书用 $\bar{z}$ 表示复共轭，和 $z^*$ 意思相同。量子力学里通常用 $z^*$。

### 例子

| $z$ | $z^*$ |
|-----|-------|
| $3 + 4i$ | $3 - 4i$ |
| $-1 + 2i$ | $-1 - 2i$ |
| $5$ | $5$（实数的共轭是自身） |
| $3i$ | $-3i$ |
| $2 - 7i$ | $2 + 7i$ |

### 关键性质：$z \cdot z^*$ 是实数

这是复共轭最重要的用途：

$$z \cdot z^* = (a + bi)(a - bi)$$

展开：

$$= a^2 - abi + abi - b^2 i^2 = a^2 - b^2(-1) = a^2 + b^2$$

结果是 $a^2 + b^2$，这是一个**非负实数**！

这个性质在量子力学中极其重要，我们会反复用到。

---

## 五、复数的模（Modulus）

$z = a + bi$ 的**模**（modulus）定义为：

$$\vert z \vert = \sqrt{a^2 + b^2}$$

等价地，由上面的性质：

$$\vert z \vert = \sqrt{z \cdot z^*}$$

模可以理解为复数的"大小"或"长度"，是一个非负实数。

**例子**：

- $\vert 3 + 4i \vert = \sqrt{9 + 16} = \sqrt{25} = 5$
- $\vert -1 + i \vert = \sqrt{1 + 1} = \sqrt{2}$
- $\vert 5 \vert = \sqrt{25} = 5$（和实数的绝对值一致）
- $\vert i \vert = \sqrt{0 + 1} = 1$

---

## 六、复平面：把复数"看见"

复数 $z = a + bi$ 可以用**平面上的一个点**来表示：

- 横轴（**实轴**）代表实部 $a$
- 纵轴（**虚轴**）代表虚部 $b$

```
虚轴 Im
  ^
  |
3 +          * z = 1 + 3i
  |
2 +
  |
1 +
  |
  +----+----+----+-----> 实轴 Re
  0    1    2    3
```

这个平面叫**复平面**（complex plane 或 Argand plane）。

在复平面上：

- **模** $\vert z \vert$ 就是点 $z$ 到原点的**距离**
- **辐角**（argument）$\theta$ 是从正实轴到点 $z$ 的连线，逆时针旋转的角度

用 $r$ 表示模，用 $\theta$ 表示辐角，可以把任意复数写成：

$$z = r(\cos\theta + i\sin\theta)$$

其中 $a = r\cos\theta$，$b = r\sin\theta$。

---

## 七、欧拉公式：量子力学最重要的公式之一

数学上有一个令人惊叹的公式——**欧拉公式**（Euler's formula）：

$$\boxed{e^{i\theta} = \cos\theta + i\sin\theta}$$

其中 $e \approx 2.718...$ 是自然常数，$\theta$ 是任意实数（角度，以弧度为单位）。

这个公式把指数函数和三角函数联系在一起，乍看很奇怪，但它是完全严格的数学事实。

利用欧拉公式，复数的极坐标形式变成：

$$z = r e^{i\theta}$$

### 几个重要的特殊值

把不同的 $\theta$ 代入 $e^{i\theta} = \cos\theta + i\sin\theta$：

| $\theta$（弧度） | $\cos\theta$ | $\sin\theta$ | $e^{i\theta}$ |
|--------|------|------|------|
| $0$ | $1$ | $0$ | $1$ |
| $\pi/2$（即 $90°$） | $0$ | $1$ | $i$ |
| $\pi$（即 $180°$） | $-1$ | $0$ | $-1$ |
| $3\pi/2$（即 $270°$） | $0$ | $-1$ | $-i$ |
| $2\pi$（即 $360°$） | $1$ | $0$ | $1$ |

所以在复平面上，$e^{i\theta}$ 是单位圆上的点，$\theta$ 就是转过的角度：

```
虚轴 Im
  ^
  |  e^{iπ/2} = i
  |
--*--------*--> 实轴 Re
 -1  e^{iπ}  1 = e^{i·0}
  |
  |  e^{i·3π/2} = -i
```

### 欧拉恒等式

当 $\theta = \pi$ 时：

$$e^{i\pi} = -1 \implies e^{i\pi} + 1 = 0$$

这个公式 $e^{i\pi} + 1 = 0$ 叫**欧拉恒等式**，把数学中最重要的五个常数（$e$、$i$、$\pi$、$1$、$0$）联系在一个等式里，被誉为"数学最美公式"。

### 为什么量子力学里到处是 $e^{i\theta}$？

因为量子力学中的波函数（描述粒子状态的函数）包含大量形如 $e^{i\theta}$ 的因子，它们代表"相位"——可以想象成一个在复平面上旋转的箭头。后面学到波函数时你会大量见到它。

---

## 八、除法（补充）

$$\frac{z_1}{z_2} = \frac{z_1 \cdot z_2^*}{z_2 \cdot z_2^*} = \frac{(a+bi)(c-di)}{c^2 + d^2}$$

技巧：分子分母同乘分母的复共轭，把分母变成实数。

**例子**：$\dfrac{1+2i}{3+4i}$

$$= \frac{(1+2i)(3-4i)}{(3+4i)(3-4i)} = \frac{3 - 4i + 6i - 8i^2}{9 + 16} = \frac{3 + 2i + 8}{25} = \frac{11 + 2i}{25} = \frac{11}{25} + \frac{2}{25}i$$

---

## 小测试

1. 计算 $(2 + 3i) + (1 - i)$
2. 计算 $(1 + i)^2$（展开后用 $i^2 = -1$ 化简）
3. 计算 $\vert 1 + i \vert$
4. 写出 $z = 1 + i$ 的复共轭 $z^*$，并验证 $z \cdot z^* = \vert z \vert^2$
5. 用欧拉公式写出 $e^{i\pi/2}$ 等于哪个复数？
6. 计算 $\dfrac{1}{i}$（提示：分子分母乘以 $i^* = -i$）

<details>
<summary>点击查看答案</summary>

1. $(2+1) + (3-1)i = 3 + 2i$

2. $(1+i)^2 = 1 + 2i + i^2 = 1 + 2i - 1 = 2i$

3. $\vert 1+i \vert = \sqrt{1^2 + 1^2} = \sqrt{2}$

4. $z^* = 1 - i$；验证：$z \cdot z^* = (1+i)(1-i) = 1 - i^2 = 1+1 = 2 = (\sqrt{2})^2 = \vert z \vert^2$ ✓

5. $e^{i\pi/2} = \cos(\pi/2) + i\sin(\pi/2) = 0 + i \cdot 1 = i$

6. $\dfrac{1}{i} = \dfrac{1 \cdot (-i)}{i \cdot (-i)} = \dfrac{-i}{-i^2} = \dfrac{-i}{1} = -i$

</details>

---

## 总结

复数是实数的扩展，核心是虚数单位 $i = \sqrt{-1}$。

最重要的三件事：

1. **复共轭** $z^* = a - bi$：把虚部符号取反，$z \cdot z^*$ 变成实数 $a^2 + b^2$
2. **模** $\vert z \vert = \sqrt{a^2+b^2}$：复数的"大小"
3. **欧拉公式** $e^{i\theta} = \cos\theta + i\sin\theta$：量子力学中无处不在

---

下一节：[01_第一章_线性向量空间.md](01_第一章_线性向量空间.md)
