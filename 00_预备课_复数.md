# 预备课：复数（Complex Numbers）

> 第一章正式开始前的数学准备。从零开始，不假设任何前置知识。

---

## 先想一个问题

什么样的数，乘以自己之后等于 $-1$？

你肯定知道：
- 正数 × 正数 = 正数
- 负数 × 负数 = 正数
- 零 × 零 = 零

所以任何实数的平方都 $\geq 0$，永远不会等于 $-1$。在实数范围里，这个问题真的无解。

---

## 数学家的做法：造一个新的数

数学家决定扩展数的范围：假装这样的数存在，给它一个名字 $i$，唯一的规则是：

$$i^2 = -1$$

这个 $i$ 叫做**虚数单位**（imaginary unit）。

$i$ 的幂次是循环的：

$$i^1 = i, \quad i^2 = -1, \quad i^3 = -i, \quad i^4 = 1, \quad i^5 = i, \ldots$$

---

## 复数的定义

把 $i$ 和实数混合相加，得到**复数**（complex number）：

$$z = a + bi$$

- $a$ 叫**实部**（real part），写作 $\operatorname{Re}(z)$
- $b$ 叫**虚部**（imaginary part），写作 $\operatorname{Im}(z)$（注意：虚部是 $b$ 本身，是实数）
- 实数是复数的特例（$b=0$）

---

## 加法

实部和实部相加，虚部和虚部相加：

$$(a + bi) + (c + di) = (a+c) + (b+d)i$$

**例**：$(3+2i)+(1-5i) = 4-3i$

---

## 乘法

当成普通代数展开，然后用 $i^2 = -1$ 化简：

$$(a+bi)(c+di) = ac + adi + bci + bdi^2 = (ac-bd) + (ad+bc)i$$

不用背公式，每次直接展开即可。

**例**：$(1+2i)(3+4i) = 3+4i+6i+8i^2 = 3+10i-8 = -5+10i$

---

## 复共轭（Complex Conjugate）⭐ 最重要

给定 $z = a+bi$，它的**复共轭**是：

$$z^* = a - bi \quad \text{（虚部符号取反，实部不变）}$$

**关键性质**：

$$z \cdot z^* = (a+bi)(a-bi) = a^2 - b^2 i^2 = a^2 + b^2 \quad \text{（非负实数！）}$$

推导每步展开：$a^2 - abi + abi - b^2 i^2$，中间两项消去，$-b^2(-1) = b^2$。

---

## 模（Modulus）：复数的"大小"

$$|z| = \sqrt{z \cdot z^*} = \sqrt{a^2 + b^2}$$

**例**：$|3+4i| = \sqrt{9+16} = 5$

---

## 复平面：把复数"看见"

$z = a+bi$ 对应复平面上坐标 $(a,b)$ 的点。横轴是实部，纵轴是虚部。模 $|z|$ 就是到原点的距离。

```
Im
↑
|   × (1+3i)
|
|         × (3+i)
+----------→ Re
```

---

## 欧拉公式（先接受，以后证明）

$$\boxed{e^{i\theta} = \cos\theta + i\sin\theta}$$

特殊值：

- $e^{i \cdot 0} = 1$
- $e^{i\pi/2} = i$
- $e^{i\pi} = -1$（即 $e^{i\pi}+1=0$，欧拉恒等式）
- $e^{i \cdot 2\pi} = 1$

几何意义：$e^{i\theta}$ 是复平面单位圆上的点，$\theta$ 是转过的角度。

---

## 练习

1. $(3+i)+(2-3i)$
2. $(1+i)^2$
3. $z = 4-3i$ 的复共轭，以及 $z\cdot z^*$
4. $|3+4i|$
5. $e^{i\pi/2}$ 等于哪个复数？

<details>
<summary>答案</summary>

1. $5 - 2i$
2. $(1+i)(1+i) = 1+2i+i^2 = 2i$
3. $z^* = 4+3i$；$z\cdot z^* = 16+9 = 25$
4. $5$
5. $i$

</details>

---

下一节：[01_第一章_线性向量空间.md](01_第一章_线性向量空间.md)
